---
status: audit
owner: project
updated: 2026-09-12
tags: [loans, portability, audit, inventory]
---

# Loans and portability declared schema inventory

Companion to [the audit](../architecture/loans-portability-audit.md) and [classified constraints](../domain/loans-portability-constraints.md). Generated by static AST inspection of the working tree, not database introspection. Includes all model declarations in Loans and data_portability; inherited fields are noted separately. SQL trigger rules and service rules are classified in the companion. A field with `null=False` is a storage requirement, not proof that its value is historically known. `blank`, defaults, `clean`, and `save` differ from database enforcement. All source links point to the file; declaration line numbers identify this audit snapshot.

Inherited `WorkspaceOwnedModel.workspace` is a non-null `Company` FK with PROTECT. Automatic IDs are supplied by Django. Abstract model inheritance is printed; follow its declaration for inherited immutability. `unique=True`, OneToOne and primary keys add uniqueness beyond the named constraints below.

## Measurements

87 model/abstract-model declarations; 871 directly declared fields; 170 named constraint declarations. This counts declarations, not independent business rules or applied database constraints.

## CollateralAppraisal

[apps/tenant_apps/loans/models/appraisals.py](../../apps/tenant_apps/loans/models/appraisals.py), line 9. Bases: `models.Model`. ordering=('collateral_item_id', 'version')

| Field | Declaration (type, optionality, relation, deletion, default, precision) |
| --- | --- |
| `workspace` (L15) | `models.ForeignKey('orgs.Company', on_delete=models.PROTECT, related_name='collateral_appraisals')` |
| `collateral_item` (L16) | `models.ForeignKey('loans.PawnCollateralItem', on_delete=models.PROTECT, related_name='appraisals')` |
| `version` (L17) | `models.PositiveIntegerField()` |
| `effective_at` (L18) | `models.DateTimeField(db_index=True)` |
| `appraised_value` (L19) | `models.DecimalField(max_digits=18, decimal_places=4)` |
| `status` (L20) | `models.CharField(max_length=12, choices=Status.choices, default=Status.APPROVED)` |
| `method` (L21) | `models.CharField(max_length=80)` |
| `evidence_reference` (L22) | `models.CharField(max_length=255, blank=True)` |
| `review_notes` (L23) | `models.TextField(blank=True)` |
| `valuation_context` (L24) | `models.JSONField(default=dict, blank=True, editable=False)` |
| `supersedes` (L25) | `models.OneToOneField('self', null=True, blank=True, on_delete=models.PROTECT, related_name='superseded_by')` |
| `created_at` (L26) | `models.DateTimeField(auto_now_add=True)` |
| `created_by` (L27) | `models.ForeignKey(settings.AUTH_USER_MODEL, null=True, blank=True, on_delete=models.PROTECT, related_name='collateral_appraisals_created')` |

Named constraints:

- L32: `models.UniqueConstraint(fields=('collateral_item', 'version'), name='loans_appraisal_item_version_uniq')`
- L33: `models.CheckConstraint(condition=Q(appraised_value__gt=0), name='loans_appraisal_value_positive')`

Methods/properties: clean (L37), save (L49), delete (L55).

## PawnLoanCommunicationConsent

[apps/tenant_apps/loans/models/communication.py](../../apps/tenant_apps/loans/models/communication.py), line 9. Bases: `models.Model`.

| Field | Declaration (type, optionality, relation, deletion, default, precision) |
| --- | --- |
| `workspace` (L12) | `models.ForeignKey('orgs.Company', on_delete=models.PROTECT, related_name='pawn_loan_communication_consents')` |
| `party` (L13) | `models.ForeignKey('party.Party', on_delete=models.PROTECT, related_name='pawn_loan_communication_consents')` |
| `channel` (L14) | `models.CharField(max_length=16, choices=enum_choices(PawnLoanNoticeChannel))` |
| `service_notices_allowed` (L15) | `models.BooleanField(default=False)` |
| `opted_out_at` (L16) | `models.DateTimeField(null=True, blank=True)` |
| `evidence` (L17) | `models.CharField(max_length=255, blank=True)` |
| `updated_by` (L18) | `models.ForeignKey(settings.AUTH_USER_MODEL, null=True, blank=True, on_delete=models.SET_NULL, related_name='pawn_loan_communication_consents_updated')` |
| `updated_at` (L19) | `models.DateTimeField(auto_now=True)` |

Named constraints:

- L22: `models.UniqueConstraint(fields=('workspace', 'party', 'channel'), name='loans_pawn_notice_consent_uniq')`

Methods/properties: clean (L24), permits_service_notice (L30), save (L33).

## PawnLoanCommunicationPolicy

[apps/tenant_apps/loans/models/communication.py](../../apps/tenant_apps/loans/models/communication.py), line 38. Bases: `models.Model`.

| Field | Declaration (type, optionality, relation, deletion, default, precision) |
| --- | --- |
| `workspace` (L41) | `models.OneToOneField('orgs.Company', on_delete=models.PROTECT, related_name='pawn_loan_communication_policy')` |
| `preferred_channel` (L44) | `models.CharField(max_length=16, choices=(('EMAIL', 'Email'), ('WHATSAPP', 'WhatsApp')), default='EMAIL')` |
| `quiet_hours_start` (L49) | `models.TimeField(null=True, blank=True)` |
| `quiet_hours_end` (L50) | `models.TimeField(null=True, blank=True)` |
| `cooldown_days` (L51) | `models.PositiveSmallIntegerField(default=0)` |
| `escalation_dpd` (L52) | `models.PositiveSmallIntegerField(default=30)` |
| `updated_by` (L53) | `models.ForeignKey(settings.AUTH_USER_MODEL, null=True, blank=True, on_delete=models.SET_NULL, related_name='pawn_loan_communication_policies_updated')` |
| `updated_at` (L57) | `models.DateTimeField(auto_now=True)` |

Methods/properties: clean (L59), save (L72).

## LoanLicense

[apps/tenant_apps/loans/models/core.py](../../apps/tenant_apps/loans/models/core.py), line 40. Bases: `models.Model`. ordering=('workspace_id', 'license_number')

| Field | Declaration (type, optionality, relation, deletion, default, precision) |
| --- | --- |
| `workspace` (L41) | `models.ForeignKey('orgs.Company', on_delete=models.PROTECT, related_name='loan_licenses')` |
| `name` (L46) | `models.CharField(max_length=255)` |
| `license_number` (L47) | `models.CharField(max_length=100)` |
| `issuing_authority` (L48) | `models.CharField(max_length=255, blank=True)` |
| `issued_on` (L49) | `models.DateField(null=True, blank=True)` |
| `expires_on` (L50) | `models.DateField(null=True, blank=True)` |
| `is_legacy_reference` (L51) | `models.BooleanField(default=False, editable=False)` |
| `is_active` (L52) | `models.BooleanField(default=True, db_index=True)` |
| `notes` (L53) | `models.TextField(blank=True)` |
| `created_at` (L54) | `models.DateTimeField(auto_now_add=True)` |
| `updated_at` (L55) | `models.DateTimeField(auto_now=True)` |
| `created_by` (L56) | `models.ForeignKey(settings.AUTH_USER_MODEL, null=True, blank=True, on_delete=models.SET_NULL, related_name='loan_licenses_created')` |
| `updated_by` (L63) | `models.ForeignKey(settings.AUTH_USER_MODEL, null=True, blank=True, on_delete=models.SET_NULL, related_name='loan_licenses_updated')` |

Named constraints:

- L74: `models.UniqueConstraint(fields=('workspace', 'license_number'), name='loans_license_workspace_number_uniq')`
- L78: `models.CheckConstraint(condition=Q(expires_on__gte=F('issued_on')), name='loans_license_dates_valid')`
- L82: `models.CheckConstraint(condition=Q(is_legacy_reference=True, is_active=False, issued_on__isnull=True, expires_on__isnull=True) | Q(is_legacy_reference=False, issued_on__isnull=False, expires_on__isnull=False), name='loans_license_validity_evidence')`

Methods/properties: __str__ (L95), clean (L98), save (L106), is_expired (L110).

## LoanSeries

[apps/tenant_apps/loans/models/core.py](../../apps/tenant_apps/loans/models/core.py), line 114. Bases: `WorkspaceOwnedModel`. ordering=('license_id', 'code')

| Field | Declaration (type, optionality, relation, deletion, default, precision) |
| --- | --- |
| `license` (L115) | `models.ForeignKey(LoanLicense, on_delete=models.PROTECT, related_name='series')` |
| `name` (L120) | `models.CharField(max_length=100)` |
| `code` (L121) | `models.CharField(max_length=32)` |
| `is_active` (L122) | `models.BooleanField(default=True, db_index=True)` |
| `created_at` (L123) | `models.DateTimeField(auto_now_add=True)` |
| `updated_at` (L124) | `models.DateTimeField(auto_now=True)` |

Named constraints:

- L129: `models.UniqueConstraint(fields=('license', 'code'), name='loans_series_license_code_uniq')`

Methods/properties: __str__ (L141).

## PawnLoanEconomicPolicy

[apps/tenant_apps/loans/models/core.py](../../apps/tenant_apps/loans/models/core.py), line 145. Bases: `models.Model`. ordering=('workspace_id', 'license_id', '-effective_from', '-id')

| Field | Declaration (type, optionality, relation, deletion, default, precision) |
| --- | --- |
| `workspace` (L146) | `models.ForeignKey('orgs.Company', on_delete=models.PROTECT, related_name='pawn_loan_economic_policies')` |
| `license` (L151) | `models.ForeignKey(LoanLicense, null=True, blank=True, on_delete=models.PROTECT, related_name='economic_policies')` |
| `valuation_method` (L158) | `models.CharField(max_length=40, choices=enum_choices(ValuationMethod), default=ValuationMethod.LOWER_OF_CALCULATED_AND_APPRAISAL.value)` |
| `maximum_ltv_ratio` (L163) | `models.DecimalField(max_digits=7, decimal_places=6, default=Decimal('0.800000'))` |
| `advance_interest_periods` (L168) | `models.PositiveSmallIntegerField(default=1)` |
| `interest_method` (L169) | `models.CharField(max_length=16, choices=enum_choices(InterestMethod), default=InterestMethod.SIMPLE.value)` |
| `partial_month_method` (L174) | `models.CharField(max_length=16, choices=enum_choices(PartialMonthMethod), default=PartialMonthMethod.FULL_MONTH.value)` |
| `partial_month_cutoff_days` (L179) | `models.PositiveSmallIntegerField(default=15)` |
| `partial_month_lower_fraction` (L180) | `models.DecimalField(max_digits=5, decimal_places=4, default=Decimal('0.5'))` |
| `capitalization_interval_periods` (L185) | `models.PositiveSmallIntegerField(default=12)` |
| `rounding_method` (L186) | `models.CharField(max_length=32, choices=enum_choices(RoundingMethod), default=RoundingMethod.PER_ACCRUAL_PERIOD.value)` |
| `currency_quantum` (L191) | `models.DecimalField(max_digits=8, decimal_places=4, default=Decimal('0.01'))` |
| `effective_from` (L196) | `models.DateField(default=timezone.localdate, db_index=True)` |
| `effective_until` (L197) | `models.DateField(null=True, blank=True)` |
| `is_active` (L198) | `models.BooleanField(default=True, db_index=True)` |
| `created_at` (L199) | `models.DateTimeField(auto_now_add=True)` |
| `created_by` (L200) | `models.ForeignKey(settings.AUTH_USER_MODEL, null=True, blank=True, on_delete=models.SET_NULL, related_name='pawn_loan_economic_policies_created')` |

Named constraints:

- L211: `models.UniqueConstraint(fields=('workspace', 'effective_from'), condition=Q(license__isnull=True), name='loans_econ_ws_date_uniq')`
- L216: `models.UniqueConstraint(fields=('license', 'effective_from'), condition=Q(license__isnull=False), name='loans_econ_license_date_uniq')`
- L221: `models.CheckConstraint(condition=Q(maximum_ltv_ratio__gt=0) & Q(maximum_ltv_ratio__lte=1), name='loans_econ_ltv_range')`
- L226: `models.CheckConstraint(condition=Q(advance_interest_periods__lte=12), name='loans_econ_advance_range')`
- L230: `models.CheckConstraint(condition=Q(partial_month_cutoff_days__gte=1) & Q(partial_month_cutoff_days__lte=30), name='loans_econ_partial_cutoff_range')`
- L235: `models.CheckConstraint(condition=Q(partial_month_lower_fraction__gt=0) & Q(partial_month_lower_fraction__lte=1), name='loans_econ_partial_fraction_range')`
- L240: `models.CheckConstraint(condition=Q(capitalization_interval_periods__gte=1), name='loans_econ_cap_interval_positive')`
- L244: `models.CheckConstraint(condition=Q(currency_quantum__gt=0), name='loans_econ_currency_quantum_positive')`
- L248: `models.CheckConstraint(condition=Q(effective_until__isnull=True) | Q(effective_until__gte=F('effective_from')), name='loans_econ_dates_valid')`

Methods/properties: clean (L261), save (L280).

## PawnMetalInterestRatePolicy

[apps/tenant_apps/loans/models/core.py](../../apps/tenant_apps/loans/models/core.py), line 285. Bases: `models.Model`. ordering=('workspace_id', 'license_id', 'metal', '-effective_from', '-id')

| Field | Declaration (type, optionality, relation, deletion, default, precision) |
| --- | --- |
| `workspace` (L286) | `models.ForeignKey('orgs.Company', on_delete=models.PROTECT, related_name='pawn_metal_interest_rate_policies')` |
| `license` (L291) | `models.ForeignKey(LoanLicense, null=True, blank=True, on_delete=models.PROTECT, related_name='metal_interest_rate_policies')` |
| `metal` (L298) | `models.CharField(max_length=16, choices=enum_choices(CollateralMetal))` |
| `monthly_interest_rate` (L299) | `models.DecimalField(max_digits=9, decimal_places=6)` |
| `effective_from` (L300) | `models.DateField(default=timezone.localdate, db_index=True)` |
| `effective_until` (L301) | `models.DateField(null=True, blank=True)` |
| `is_active` (L302) | `models.BooleanField(default=True, db_index=True)` |
| `created_at` (L303) | `models.DateTimeField(auto_now_add=True)` |
| `created_by` (L304) | `models.ForeignKey(settings.AUTH_USER_MODEL, null=True, blank=True, on_delete=models.SET_NULL, related_name='pawn_metal_interest_rate_policies_created')` |

Named constraints:

- L315: `models.UniqueConstraint(fields=('workspace', 'metal', 'effective_from'), condition=Q(license__isnull=True), name='loans_rate_ws_metal_date_uniq')`
- L320: `models.UniqueConstraint(fields=('license', 'metal', 'effective_from'), condition=Q(license__isnull=False), name='loans_rate_license_date_uniq')`
- L325: `models.CheckConstraint(condition=Q(monthly_interest_rate__gte=0) & Q(monthly_interest_rate__lte=100), name='loans_rate_percent_range')`
- L330: `models.CheckConstraint(condition=Q(effective_until__isnull=True) | Q(effective_until__gte=F('effective_from')), name='loans_rate_dates_valid')`

Methods/properties: clean (L343), save (L347).

## PawnLoanFeePolicy

[apps/tenant_apps/loans/models/core.py](../../apps/tenant_apps/loans/models/core.py), line 352. Bases: `models.Model`. ordering=('workspace_id', 'license_id', 'code', '-effective_from', '-id')

| Field | Declaration (type, optionality, relation, deletion, default, precision) |
| --- | --- |
| `workspace` (L353) | `models.ForeignKey('orgs.Company', on_delete=models.PROTECT, related_name='pawn_loan_fee_policies')` |
| `license` (L358) | `models.ForeignKey(LoanLicense, null=True, blank=True, on_delete=models.PROTECT, related_name='fee_policies')` |
| `code` (L365) | `models.CharField(max_length=32)` |
| `name` (L366) | `models.CharField(max_length=100)` |
| `calculation_type` (L367) | `models.CharField(max_length=16, choices=enum_choices(FeeCalculationType))` |
| `value` (L371) | `models.DecimalField(max_digits=18, decimal_places=6)` |
| `deducted_at_disbursal` (L372) | `models.BooleanField(default=True)` |
| `effective_from` (L373) | `models.DateField(default=timezone.localdate, db_index=True)` |
| `effective_until` (L374) | `models.DateField(null=True, blank=True)` |
| `is_active` (L375) | `models.BooleanField(default=True, db_index=True)` |
| `created_at` (L376) | `models.DateTimeField(auto_now_add=True)` |
| `created_by` (L377) | `models.ForeignKey(settings.AUTH_USER_MODEL, null=True, blank=True, on_delete=models.SET_NULL, related_name='pawn_loan_fee_policies_created')` |

Named constraints:

- L388: `models.UniqueConstraint(fields=('workspace', 'code', 'effective_from'), condition=Q(license__isnull=True), name='loans_fee_ws_code_date_uniq')`
- L393: `models.UniqueConstraint(fields=('license', 'code', 'effective_from'), condition=Q(license__isnull=False), name='loans_fee_license_date_uniq')`
- L398: `models.CheckConstraint(condition=Q(value__gte=0), name='loans_fee_value_nonnegative')`
- L402: `models.CheckConstraint(condition=Q(effective_until__isnull=True) | Q(effective_until__gte=F('effective_from')), name='loans_fee_dates_valid')`

Methods/properties: clean (L415), save (L425).

## LoanNumberSequence

[apps/tenant_apps/loans/models/core.py](../../apps/tenant_apps/loans/models/core.py), line 448. Bases: `WorkspaceOwnedModel`. ordering=('series_id', 'document_kind')

| Field | Declaration (type, optionality, relation, deletion, default, precision) |
| --- | --- |
| `series` (L449) | `models.ForeignKey(LoanSeries, on_delete=models.PROTECT, related_name='number_sequences')` |
| `document_kind` (L454) | `models.CharField(max_length=32, choices=enum_choices(LoanDocumentKind))` |
| `prefix` (L458) | `models.CharField(max_length=24)` |
| `width` (L459) | `models.PositiveSmallIntegerField(default=5)` |
| `next_number` (L460) | `models.PositiveBigIntegerField(default=1)` |
| `maximum_number` (L461) | `models.PositiveBigIntegerField(default=10000)` |
| `is_active` (L462) | `models.BooleanField(default=True, db_index=True)` |
| `created_at` (L463) | `models.DateTimeField(auto_now_add=True)` |
| `updated_at` (L464) | `models.DateTimeField(auto_now=True)` |
| `updated_by` (L465) | `models.ForeignKey(settings.AUTH_USER_MODEL, null=True, blank=True, on_delete=models.SET_NULL, related_name='loan_number_sequences_updated')` |

Named constraints:

- L476: `models.UniqueConstraint(fields=('series', 'document_kind'), name='loans_sequence_series_kind_uniq')`
- L480: `models.CheckConstraint(condition=Q(width__gt=0), name='loans_sequence_width_positive')`
- L484: `models.CheckConstraint(condition=Q(next_number__gt=0), name='loans_sequence_next_positive')`
- L488: `models.CheckConstraint(condition=Q(maximum_number__gt=0), name='loans_sequence_max_positive')`

Methods/properties: clean (L500), save (L512).

## PawnLoan

[apps/tenant_apps/loans/models/core.py](../../apps/tenant_apps/loans/models/core.py), line 517. Bases: `models.Model`. ordering=('loan_date', 'loan_number')

| Field | Declaration (type, optionality, relation, deletion, default, precision) |
| --- | --- |
| `workspace` (L518) | `models.ForeignKey('orgs.Company', on_delete=models.PROTECT, related_name='pawn_loans')` |
| `license` (L523) | `models.ForeignKey(LoanLicense, on_delete=models.PROTECT, related_name='pawn_loans')` |
| `license_revision` (L528) | `models.ForeignKey('loans.LoanLicenseRevision', null=True, blank=True, on_delete=models.PROTECT, related_name='pawn_loans')` |
| `series` (L535) | `models.ForeignKey(LoanSeries, on_delete=models.PROTECT, related_name='pawn_loans')` |
| `borrower` (L540) | `models.ForeignKey('party.Party', on_delete=models.PROTECT, related_name='pawn_loans')` |
| `product_version` (L545) | `models.ForeignKey('loans.LoanProductVersion', on_delete=models.PROTECT, related_name='pawn_loans')` |
| `loan_number` (L550) | `models.CharField(max_length=64)` |
| `state` (L551) | `models.CharField(max_length=16, choices=enum_choices(PawnLoanState), default=PawnLoanState.DRAFT.value, db_index=True)` |
| `principal_amount` (L557) | `models.DecimalField(max_digits=18, decimal_places=2)` |
| `monthly_interest_rate` (L558) | `models.DecimalField(max_digits=9, decimal_places=6)` |
| `loan_date` (L559) | `models.DateField(default=timezone.localdate, db_index=True)` |
| `tenure_months` (L560) | `models.PositiveIntegerField(default=3)` |
| `created_at` (L561) | `models.DateTimeField(auto_now_add=True)` |
| `updated_at` (L562) | `models.DateTimeField(auto_now=True)` |
| `created_by` (L563) | `models.ForeignKey(settings.AUTH_USER_MODEL, null=True, blank=True, on_delete=models.SET_NULL, related_name='pawn_loans_created')` |
| `updated_by` (L570) | `models.ForeignKey(settings.AUTH_USER_MODEL, null=True, blank=True, on_delete=models.SET_NULL, related_name='pawn_loans_updated')` |

Named constraints:

- L581: `models.UniqueConstraint(fields=('workspace', 'loan_number'), name='loans_pawn_workspace_number_uniq')`
- L585: `models.CheckConstraint(condition=Q(principal_amount__gt=0), name='loans_pawn_principal_positive')`
- L589: `models.CheckConstraint(condition=Q(monthly_interest_rate__gte=0), name='loans_pawn_rate_nonnegative')`
- L593: `models.CheckConstraint(condition=Q(tenure_months__gt=0), name='loans_pawn_tenure_positive')`

Methods/properties: clean (L609), save (L636), __str__ (L651).

## PawnCollateralItem

[apps/tenant_apps/loans/models/core.py](../../apps/tenant_apps/loans/models/core.py), line 655. Bases: `WorkspaceOwnedModel`. ordering=('loan_id', 'id')

| Field | Declaration (type, optionality, relation, deletion, default, precision) |
| --- | --- |
| `public_id` (L656) | `models.UUIDField(default=uuid.uuid4, unique=True, editable=False)` |
| `loan` (L657) | `models.ForeignKey(PawnLoan, on_delete=models.PROTECT, related_name='collateral_items')` |
| `description` (L662) | `models.CharField(max_length=255)` |
| `metal` (L663) | `models.CharField(max_length=16, choices=enum_choices(CollateralMetal))` |
| `gross_weight` (L666) | `models.DecimalField(max_digits=14, decimal_places=4, null=True)` |
| `net_weight` (L667) | `models.DecimalField(max_digits=14, decimal_places=4)` |
| `purity_percentage` (L668) | `models.DecimalField(max_digits=7, decimal_places=4)` |
| `latest_appraised_value` (L669) | `models.DecimalField(max_digits=18, decimal_places=2, null=True, blank=True)` |
| `allocated_principal` (L675) | `models.DecimalField(max_digits=18, decimal_places=2, null=True, blank=True)` |
| `monthly_interest_rate` (L681) | `models.DecimalField(max_digits=9, decimal_places=6, null=True, blank=True)` |
| `interest_rate_policy` (L687) | `models.ForeignKey(PawnMetalInterestRatePolicy, null=True, blank=True, on_delete=models.PROTECT, related_name='collateral_items')` |
| `custody_state` (L694) | `models.CharField(max_length=32, choices=enum_choices(CollateralCustodyState), default=CollateralCustodyState.IN_VAULT.value, db_index=True)` |
| `current_storage_location` (L700) | `models.ForeignKey('loans.PawnStorageLocation', null=True, blank=True, on_delete=models.PROTECT, related_name='current_collateral_items')` |
| `renewed_from` (L707) | `models.OneToOneField('self', null=True, blank=True, on_delete=models.PROTECT, related_name='renewed_as')` |
| `created_at` (L714) | `models.DateTimeField(auto_now_add=True)` |
| `updated_at` (L715) | `models.DateTimeField(auto_now=True)` |

Named constraints:

- L720: `models.CheckConstraint(condition=Q(gross_weight__gt=0), name='loans_item_gross_positive')`
- L724: `models.CheckConstraint(condition=Q(net_weight__gt=0), name='loans_item_net_positive')`
- L728: `models.CheckConstraint(condition=Q(gross_weight__gte=F('net_weight')), name='loans_item_gross_gte_net')`
- L732: `models.CheckConstraint(condition=Q(purity_percentage__gt=0) & Q(purity_percentage__lte=100), name='loans_item_purity_range')`
- L737: `models.CheckConstraint(condition=Q(latest_appraised_value__isnull=True) | Q(latest_appraised_value__gt=0), name='loans_item_appraisal_positive')`
- L742: `models.CheckConstraint(condition=Q(allocated_principal__isnull=True) | Q(allocated_principal__gt=0), name='loans_item_allocation_positive')`
- L747: `models.CheckConstraint(condition=Q(monthly_interest_rate__isnull=True) | Q(monthly_interest_rate__gte=0) & Q(monthly_interest_rate__lte=100), name='loans_item_rate_range')`

Methods/properties: clean (L763), save (L789).

## LoanPolicySnapshot

[apps/tenant_apps/loans/models/core.py](../../apps/tenant_apps/loans/models/core.py), line 794. Bases: `WorkspaceOwnedModel`.

| Field | Declaration (type, optionality, relation, deletion, default, precision) |
| --- | --- |
| `loan` (L795) | `models.OneToOneField(PawnLoan, on_delete=models.PROTECT, related_name='policy_snapshot')` |
| `policy_version` (L800) | `models.PositiveSmallIntegerField(default=1)` |
| `interest_method` (L801) | `models.CharField(max_length=16, choices=enum_choices(InterestMethod))` |
| `partial_month_method` (L805) | `models.CharField(max_length=16, choices=enum_choices(PartialMonthMethod))` |
| `partial_month_cutoff_days` (L809) | `models.PositiveSmallIntegerField(default=15)` |
| `partial_month_lower_fraction` (L810) | `models.DecimalField(max_digits=5, decimal_places=4, default=Decimal('0.5'))` |
| `capitalization_interval_periods` (L815) | `models.PositiveSmallIntegerField(default=12)` |
| `valuation_method` (L816) | `models.CharField(max_length=48, choices=enum_choices(ValuationMethod))` |
| `maximum_ltv_ratio` (L820) | `models.DecimalField(max_digits=5, decimal_places=4, default=Decimal('0.80'))` |
| `rounding_method` (L825) | `models.CharField(max_length=32, choices=enum_choices(RoundingMethod))` |
| `currency_quantum` (L829) | `models.DecimalField(max_digits=8, decimal_places=4, default=Decimal('0.01'))` |
| `created_at` (L834) | `models.DateTimeField(auto_now_add=True)` |

Named constraints:

- L838: `models.CheckConstraint(condition=Q(policy_version__gt=0), name='loans_policy_version_positive')`
- L842: `models.CheckConstraint(condition=Q(maximum_ltv_ratio__gt=0) & Q(maximum_ltv_ratio__lte=1), name='loans_policy_ltv_range')`

Methods/properties: clean (L849), save (L867).

## LoanChangeLog

[apps/tenant_apps/loans/models/core.py](../../apps/tenant_apps/loans/models/core.py), line 872. Bases: `WorkspaceOwnedModel`. ordering=('loan_id', 'created_at', 'id')

| Field | Declaration (type, optionality, relation, deletion, default, precision) |
| --- | --- |
| `loan` (L873) | `models.ForeignKey(PawnLoan, on_delete=models.PROTECT, related_name='change_log')` |
| `event_kind` (L878) | `models.CharField(max_length=40, choices=enum_choices(PawnLoanEventKind))` |
| `from_state` (L882) | `models.CharField(max_length=16, choices=enum_choices(PawnLoanState), blank=True)` |
| `to_state` (L887) | `models.CharField(max_length=16, choices=enum_choices(PawnLoanState), blank=True)` |
| `reason` (L892) | `models.TextField(blank=True)` |
| `metadata` (L893) | `models.JSONField(default=dict, blank=True)` |
| `actor` (L894) | `models.ForeignKey(settings.AUTH_USER_MODEL, null=True, blank=True, on_delete=models.SET_NULL, related_name='pawn_loan_changes')` |
| `created_at` (L901) | `models.DateTimeField(auto_now_add=True, db_index=True)` |

Methods/properties: none declared; inherited behavior still applies.

## PawnLoanApprovalSnapshot

[apps/tenant_apps/loans/models/core.py](../../apps/tenant_apps/loans/models/core.py), line 913. Bases: `WorkspaceOwnedModel`. ordering=('loan_id', 'version')

| Field | Declaration (type, optionality, relation, deletion, default, precision) |
| --- | --- |
| `loan` (L914) | `models.ForeignKey(PawnLoan, on_delete=models.PROTECT, related_name='approval_snapshots')` |
| `version` (L919) | `models.PositiveIntegerField()` |
| `payload` (L920) | `models.JSONField()` |
| `fingerprint` (L921) | `models.CharField(max_length=64)` |
| `approved_by` (L922) | `models.ForeignKey(settings.AUTH_USER_MODEL, null=True, blank=True, on_delete=models.SET_NULL, related_name='pawn_loan_approvals')` |
| `approved_at` (L929) | `models.DateTimeField(auto_now_add=True)` |

Named constraints:

- L934: `models.UniqueConstraint(fields=('loan', 'version'), name='loans_approval_loan_version_uniq')`
- L938: `models.CheckConstraint(condition=Q(version__gt=0), name='loans_approval_version_positive')`

Methods/properties: save (L944), delete (L949).

## PawnLoanEvent

[apps/tenant_apps/loans/models/core.py](../../apps/tenant_apps/loans/models/core.py), line 953. Bases: `WorkspaceOwnedModel`. ordering=('loan_id', 'created_at', 'id')

| Field | Declaration (type, optionality, relation, deletion, default, precision) |
| --- | --- |
| `loan` (L955) | `models.ForeignKey(PawnLoan, on_delete=models.PROTECT, related_name='loan_events')` |
| `event_kind` (L960) | `models.CharField(max_length=32, choices=enum_choices(TransactionKind))` |
| `effective_date` (L964) | `models.DateField(db_index=True)` |
| `payload` (L965) | `models.JSONField()` |
| `payload_fingerprint` (L966) | `models.CharField(max_length=64)` |
| `idempotency_key` (L967) | `models.CharField(max_length=180, unique=True)` |
| `reversal_of` (L968) | `models.OneToOneField('self', null=True, blank=True, on_delete=models.PROTECT, related_name='reversed_by_event')` |
| `created_by` (L975) | `models.ForeignKey(settings.AUTH_USER_MODEL, null=True, blank=True, on_delete=models.SET_NULL, related_name='pawn_loan_events')` |
| `created_at` (L982) | `models.DateTimeField(auto_now_add=True)` |

Named constraints:

- L993: `models.CheckConstraint(condition=Q(event_kind=TransactionKind.REVERSAL.value, reversal_of__isnull=False) | ~Q(event_kind=TransactionKind.REVERSAL.value) & Q(reversal_of__isnull=True), name='loans_event_reversal_link_valid')`
- L1006: `models.UniqueConstraint(fields=('loan',), condition=Q(event_kind='MIGRATION_OPENING'), name='loans_one_migration_opening')`

Methods/properties: none declared; inherited behavior still applies.

## PawnLoanDisbursalSnapshot

[apps/tenant_apps/loans/models/core.py](../../apps/tenant_apps/loans/models/core.py), line 1011. Bases: `WorkspaceOwnedModel`. ordering=('loan_id',)

| Field | Declaration (type, optionality, relation, deletion, default, precision) |
| --- | --- |
| `loan` (L1014) | `models.OneToOneField(PawnLoan, on_delete=models.PROTECT, related_name='disbursal_snapshot')` |
| `approval_snapshot` (L1019) | `models.ForeignKey(PawnLoanApprovalSnapshot, on_delete=models.PROTECT, related_name='disbursal_snapshots')` |
| `policy_snapshot` (L1024) | `models.OneToOneField(LoanPolicySnapshot, on_delete=models.PROTECT, related_name='disbursal_snapshot')` |
| `loan_event` (L1029) | `models.OneToOneField(PawnLoanEvent, on_delete=models.PROTECT, related_name='disbursal_snapshot')` |
| `gross_principal` (L1034) | `models.DecimalField(max_digits=18, decimal_places=4)` |
| `monthly_interest` (L1035) | `models.DecimalField(max_digits=18, decimal_places=4)` |
| `advance_interest_periods` (L1036) | `models.PositiveSmallIntegerField(default=0)` |
| `advance_interest` (L1037) | `models.DecimalField(max_digits=18, decimal_places=4)` |
| `deducted_fees` (L1038) | `models.DecimalField(max_digits=18, decimal_places=4)` |
| `net_disbursed` (L1039) | `models.DecimalField(max_digits=18, decimal_places=4)` |
| `evidence` (L1040) | `models.JSONField()` |
| `created_by` (L1041) | `models.ForeignKey(settings.AUTH_USER_MODEL, null=True, blank=True, on_delete=models.SET_NULL, related_name='pawn_loan_disbursal_snapshots')` |
| `created_at` (L1048) | `models.DateTimeField(auto_now_add=True)` |

Named constraints:

- L1053: `models.CheckConstraint(condition=Q(gross_principal__gt=0), name='loans_disbursal_gross_positive')`
- L1057: `models.CheckConstraint(condition=Q(monthly_interest__gte=0) & Q(advance_interest__gte=0) & Q(deducted_fees__gte=0) & Q(net_disbursed__gt=0), name='loans_disbursal_amounts_valid')`

Methods/properties: clean (L1066), save (L1090), delete (L1096).

## PawnLoanInterestAccrual

[apps/tenant_apps/loans/models/core.py](../../apps/tenant_apps/loans/models/core.py), line 1100. Bases: `WorkspaceOwnedModel`. ordering=('loan_id', 'period_number')

| Field | Declaration (type, optionality, relation, deletion, default, precision) |
| --- | --- |
| `loan` (L1103) | `models.ForeignKey(PawnLoan, on_delete=models.PROTECT, related_name='interest_accruals')` |
| `period_number` (L1108) | `models.PositiveIntegerField()` |
| `period_start` (L1109) | `models.DateField()` |
| `period_end` (L1110) | `models.DateField(db_index=True)` |
| `period_fraction` (L1111) | `models.DecimalField(max_digits=8, decimal_places=4)` |
| `calculation_base` (L1112) | `models.DecimalField(max_digits=30, decimal_places=12)` |
| `unrounded_interest` (L1113) | `models.DecimalField(max_digits=30, decimal_places=12)` |
| `recognized_interest` (L1114) | `models.DecimalField(max_digits=18, decimal_places=4)` |
| `loan_event` (L1115) | `models.OneToOneField(PawnLoanEvent, null=True, blank=True, on_delete=models.PROTECT, related_name='interest_accrual')` |
| `finalized_by` (L1122) | `models.ForeignKey(settings.AUTH_USER_MODEL, null=True, blank=True, on_delete=models.SET_NULL, related_name='pawn_loan_interest_accruals')` |
| `finalized_at` (L1129) | `models.DateTimeField(auto_now_add=True)` |

Named constraints:

- L1134: `models.UniqueConstraint(fields=('loan', 'period_number'), name='loans_accrual_loan_period_uniq')`
- L1138: `models.CheckConstraint(condition=Q(period_number__gt=0), name='loans_accrual_period_positive')`
- L1142: `models.CheckConstraint(condition=Q(period_end__gte=F('period_start')), name='loans_accrual_dates_ordered')`
- L1146: `models.CheckConstraint(condition=Q(period_fraction__gt=0) & Q(period_fraction__lte=1), name='loans_accrual_fraction_range')`
- L1150: `models.CheckConstraint(condition=Q(calculation_base__gte=0) & Q(unrounded_interest__gte=0) & Q(recognized_interest__gte=0), name='loans_accrual_amounts_nonnegative')`

Methods/properties: save (L1158), delete (L1163).

## PawnLoanInterestAccrualLine

[apps/tenant_apps/loans/models/core.py](../../apps/tenant_apps/loans/models/core.py), line 1167. Bases: `WorkspaceOwnedModel`. ordering=('accrual_id', 'collateral_item_id')

| Field | Declaration (type, optionality, relation, deletion, default, precision) |
| --- | --- |
| `accrual` (L1170) | `models.ForeignKey(PawnLoanInterestAccrual, on_delete=models.PROTECT, related_name='lines')` |
| `collateral_item` (L1175) | `models.ForeignKey(PawnCollateralItem, on_delete=models.PROTECT, related_name='interest_accrual_lines')` |
| `principal_base` (L1180) | `models.DecimalField(max_digits=30, decimal_places=12)` |
| `monthly_interest_rate` (L1181) | `models.DecimalField(max_digits=12, decimal_places=6)` |
| `period_fraction` (L1182) | `models.DecimalField(max_digits=8, decimal_places=4)` |
| `unrounded_interest` (L1183) | `models.DecimalField(max_digits=30, decimal_places=12)` |
| `calculated_interest` (L1184) | `models.DecimalField(max_digits=18, decimal_places=4)` |
| `advance_interest_applied` (L1185) | `models.DecimalField(max_digits=18, decimal_places=4)` |
| `recognized_interest` (L1186) | `models.DecimalField(max_digits=18, decimal_places=4)` |

Named constraints:

- L1191: `models.UniqueConstraint(fields=('accrual', 'collateral_item'), name='loans_accrual_item_uniq')`
- L1195: `models.CheckConstraint(condition=Q(principal_base__gte=0) & Q(monthly_interest_rate__gte=0) & Q(unrounded_interest__gte=0) & Q(calculated_interest__gte=0) & Q(advance_interest_applied__gte=0) & Q(recognized_interest__gte=0), name='loans_accrual_line_amounts_valid')`
- L1204: `models.CheckConstraint(condition=Q(period_fraction__gt=0) & Q(period_fraction__lte=1), name='loans_accrual_line_fraction_valid')`

Methods/properties: clean (L1210), save (L1228), delete (L1234).

## PawnLoanRepaymentAllocationLine

[apps/tenant_apps/loans/models/core.py](../../apps/tenant_apps/loans/models/core.py), line 1238. Bases: `WorkspaceOwnedModel`. ordering=('loan_event_id', 'allocation_order')

| Field | Declaration (type, optionality, relation, deletion, default, precision) |
| --- | --- |
| `loan_event` (L1241) | `models.ForeignKey(PawnLoanEvent, on_delete=models.PROTECT, related_name='repayment_allocation_lines')` |
| `collateral_item` (L1246) | `models.ForeignKey(PawnCollateralItem, on_delete=models.PROTECT, related_name='repayment_allocation_lines')` |
| `allocation_order` (L1251) | `models.PositiveSmallIntegerField()` |
| `monthly_interest_rate` (L1252) | `models.DecimalField(max_digits=12, decimal_places=6)` |
| `balance_before` (L1253) | `models.DecimalField(max_digits=18, decimal_places=4)` |
| `principal_applied` (L1254) | `models.DecimalField(max_digits=18, decimal_places=4)` |
| `balance_after` (L1255) | `models.DecimalField(max_digits=18, decimal_places=4)` |

Named constraints:

- L1260: `models.UniqueConstraint(fields=('loan_event', 'collateral_item'), name='loans_repayment_event_item_uniq')`
- L1264: `models.UniqueConstraint(fields=('loan_event', 'allocation_order'), name='loans_repayment_event_order_uniq')`
- L1268: `models.CheckConstraint(condition=Q(allocation_order__gt=0), name='loans_repayment_order_positive')`
- L1272: `models.CheckConstraint(condition=Q(monthly_interest_rate__gte=0) & Q(balance_before__gte=0) & Q(principal_applied__gte=0) & Q(balance_after__gte=0), name='loans_repayment_line_amounts_valid')`

Methods/properties: clean (L1281), save (L1302), delete (L1308).

## PawnLoanPrincipalClosingLine

[apps/tenant_apps/loans/models/core.py](../../apps/tenant_apps/loans/models/core.py), line 1312. Bases: `WorkspaceOwnedModel`. ordering=('loan_event_id', 'allocation_order')

| Field | Declaration (type, optionality, relation, deletion, default, precision) |
| --- | --- |
| `loan_event` (L1315) | `models.ForeignKey(PawnLoanEvent, on_delete=models.PROTECT, related_name='principal_closing_lines')` |
| `collateral_item` (L1320) | `models.ForeignKey(PawnCollateralItem, on_delete=models.PROTECT, related_name='principal_closing_lines')` |
| `allocation_order` (L1325) | `models.PositiveSmallIntegerField()` |
| `monthly_interest_rate` (L1326) | `models.DecimalField(max_digits=12, decimal_places=6)` |
| `balance_before` (L1327) | `models.DecimalField(max_digits=18, decimal_places=4)` |
| `principal_settled` (L1328) | `models.DecimalField(max_digits=18, decimal_places=4)` |
| `balance_after` (L1329) | `models.DecimalField(max_digits=18, decimal_places=4)` |

Named constraints:

- L1334: `models.UniqueConstraint(fields=('loan_event', 'collateral_item'), name='loans_close_event_item_uniq')`
- L1338: `models.UniqueConstraint(fields=('loan_event', 'allocation_order'), name='loans_close_event_order_uniq')`
- L1342: `models.CheckConstraint(condition=Q(allocation_order__gt=0), name='loans_close_order_positive')`
- L1346: `models.CheckConstraint(condition=Q(monthly_interest_rate__gte=0) & Q(balance_before__gte=0) & Q(principal_settled__gte=0) & Q(balance_after__gte=0), name='loans_close_line_amounts_valid')`

Methods/properties: clean (L1357), save (L1379), delete (L1385).

## PawnLoanPrincipalOpeningLine

[apps/tenant_apps/loans/models/core.py](../../apps/tenant_apps/loans/models/core.py), line 1389. Bases: `WorkspaceOwnedModel`. ordering=('loan_event_id', 'allocation_order')

| Field | Declaration (type, optionality, relation, deletion, default, precision) |
| --- | --- |
| `loan_event` (L1392) | `models.ForeignKey(PawnLoanEvent, on_delete=models.PROTECT, related_name='principal_opening_lines')` |
| `collateral_item` (L1397) | `models.OneToOneField(PawnCollateralItem, on_delete=models.PROTECT, related_name='principal_opening_line')` |
| `predecessor_collateral_item` (L1402) | `models.ForeignKey(PawnCollateralItem, null=True, blank=True, on_delete=models.PROTECT, related_name='successor_principal_opening_lines')` |
| `allocation_order` (L1409) | `models.PositiveSmallIntegerField()` |
| `monthly_interest_rate` (L1410) | `models.DecimalField(max_digits=12, decimal_places=6)` |
| `principal_opened` (L1411) | `models.DecimalField(max_digits=18, decimal_places=4)` |

Named constraints:

- L1416: `models.UniqueConstraint(fields=('loan_event', 'allocation_order'), name='loans_open_event_order_uniq')`
- L1420: `models.CheckConstraint(condition=Q(allocation_order__gt=0), name='loans_open_order_positive')`
- L1424: `models.CheckConstraint(condition=Q(monthly_interest_rate__gte=0) & Q(principal_opened__gt=0), name='loans_open_line_amounts_valid')`

Methods/properties: clean (L1431), save (L1452), delete (L1458).

## PawnLoanRelease

[apps/tenant_apps/loans/models/core.py](../../apps/tenant_apps/loans/models/core.py), line 1462. Bases: `models.Model`. ordering=('loan_id', 'effective_date', 'id')

| Field | Declaration (type, optionality, relation, deletion, default, precision) |
| --- | --- |
| `workspace` (L1465) | `models.ForeignKey('orgs.Company', on_delete=models.PROTECT, related_name='pawn_loan_releases')` |
| `loan` (L1470) | `models.ForeignKey(PawnLoan, on_delete=models.PROTECT, related_name='releases')` |
| `release_number` (L1475) | `models.CharField(max_length=64)` |
| `request_key` (L1476) | `models.CharField(max_length=120)` |
| `effective_date` (L1477) | `models.DateField(db_index=True)` |
| `is_full_release` (L1478) | `models.BooleanField(default=True)` |
| `settlement_amount` (L1479) | `models.DecimalField(max_digits=18, decimal_places=4)` |
| `principal_amount` (L1480) | `models.DecimalField(max_digits=18, decimal_places=4)` |
| `interest_amount` (L1481) | `models.DecimalField(max_digits=18, decimal_places=4)` |
| `fee_amount` (L1482) | `models.DecimalField(max_digits=18, decimal_places=4)` |
| `valuation_snapshot` (L1483) | `models.JSONField(default=dict)` |
| `loan_event` (L1484) | `models.OneToOneField(PawnLoanEvent, on_delete=models.PROTECT, related_name='release')` |
| `catch_up_accrual` (L1489) | `models.OneToOneField(PawnLoanInterestAccrual, null=True, blank=True, on_delete=models.PROTECT, related_name='release_catch_up')` |
| `created_by` (L1496) | `models.ForeignKey(settings.AUTH_USER_MODEL, null=True, blank=True, on_delete=models.SET_NULL, related_name='pawn_loan_releases_created')` |
| `created_at` (L1503) | `models.DateTimeField(auto_now_add=True)` |

Named constraints:

- L1508: `models.UniqueConstraint(fields=('workspace', 'release_number'), name='loans_release_workspace_number_uniq')`
- L1512: `models.UniqueConstraint(fields=('loan', 'request_key'), name='loans_release_loan_request_uniq')`
- L1516: `models.CheckConstraint(condition=Q(settlement_amount__gte=0) & Q(principal_amount__gte=0) & Q(interest_amount__gte=0) & Q(fee_amount__gte=0), name='loans_release_amounts_nonnegative')`

Methods/properties: interest_concession_amount (L1528), interest_concession_reason (L1532), save (L1535), clean (L1541), delete (L1564).

## PawnLoanReleaseItem

[apps/tenant_apps/loans/models/core.py](../../apps/tenant_apps/loans/models/core.py), line 1568. Bases: `WorkspaceOwnedModel`. ordering=('release_id', 'id')

| Field | Declaration (type, optionality, relation, deletion, default, precision) |
| --- | --- |
| `release` (L1569) | `models.ForeignKey(PawnLoanRelease, on_delete=models.PROTECT, related_name='items')` |
| `collateral_item` (L1574) | `models.ForeignKey(PawnCollateralItem, on_delete=models.PROTECT, related_name='release_items')` |
| `valuation_snapshot` (L1579) | `models.JSONField(default=dict)` |
| `returned_at` (L1580) | `models.DateTimeField(default=timezone.now)` |

Named constraints:

- L1585: `models.UniqueConstraint(fields=('release', 'collateral_item'), name='loans_release_item_uniq')`

Methods/properties: save (L1591), clean (L1597), delete (L1608).

## PawnCollateralCustodyEvent

[apps/tenant_apps/loans/models/core.py](../../apps/tenant_apps/loans/models/core.py), line 1612. Bases: `WorkspaceOwnedModel`. ordering=('collateral_item_id', 'created_at', 'id')

| Field | Declaration (type, optionality, relation, deletion, default, precision) |
| --- | --- |
| `collateral_item` (L1613) | `models.ForeignKey(PawnCollateralItem, on_delete=models.PROTECT, related_name='custody_history')` |
| `release` (L1618) | `models.ForeignKey(PawnLoanRelease, null=True, blank=True, on_delete=models.PROTECT, related_name='custody_events')` |
| `auction` (L1625) | `models.ForeignKey('PawnLoanAuction', null=True, blank=True, on_delete=models.PROTECT, related_name='custody_events')` |
| `renewal` (L1632) | `models.ForeignKey('PawnLoanRenewal', null=True, blank=True, on_delete=models.PROTECT, related_name='custody_events')` |
| `funding_pledge` (L1639) | `models.ForeignKey('FundingPledge', null=True, blank=True, on_delete=models.PROTECT, related_name='custody_events')` |
| `funding_return` (L1646) | `models.ForeignKey('FundingReturn', null=True, blank=True, on_delete=models.PROTECT, related_name='custody_events')` |
| `funding_pledge_reversal` (L1653) | `models.ForeignKey('FundingPledgeReversal', null=True, blank=True, on_delete=models.PROTECT, related_name='custody_events')` |
| `funding_return_reversal` (L1660) | `models.ForeignKey('FundingReturnReversal', null=True, blank=True, on_delete=models.PROTECT, related_name='custody_events')` |
| `release_reversal` (L1667) | `models.ForeignKey('PawnLoanReleaseReversal', null=True, blank=True, on_delete=models.PROTECT, related_name='custody_events')` |
| `auction_reversal` (L1674) | `models.ForeignKey('PawnLoanAuctionReversal', null=True, blank=True, on_delete=models.PROTECT, related_name='custody_events')` |
| `renewal_reversal` (L1681) | `models.ForeignKey('PawnLoanRenewalReversal', null=True, blank=True, on_delete=models.PROTECT, related_name='custody_events')` |
| `from_state` (L1688) | `models.CharField(max_length=32, choices=enum_choices(CollateralCustodyState))` |
| `to_state` (L1692) | `models.CharField(max_length=32, choices=enum_choices(CollateralCustodyState))` |
| `effective_date` (L1696) | `models.DateField(db_index=True)` |
| `actor` (L1697) | `models.ForeignKey(settings.AUTH_USER_MODEL, null=True, blank=True, on_delete=models.SET_NULL, related_name='pawn_collateral_custody_events')` |
| `created_at` (L1704) | `models.DateTimeField(auto_now_add=True)` |

Named constraints:

- L1709: `models.CheckConstraint(condition=~Q(from_state=F('to_state')), name='loans_custody_state_changes')`
- L1713: `models.UniqueConstraint(fields=('funding_pledge_reversal', 'collateral_item'), name='loans_funding_pledge_custody_reversal_uniq')`
- L1717: `models.UniqueConstraint(fields=('funding_return_reversal', 'collateral_item'), name='loans_funding_return_custody_reversal_uniq')`
- L1721: `models.CheckConstraint(condition=Q(release__isnull=False) & Q(auction__isnull=True) & Q(renewal__isnull=True) & Q(funding_pledge__isnull=True) & Q(funding_return__isnull=True) | Q(release__isnull=True) & Q(auction__isnull=False) & Q(renewal__isnull=True) & Q(funding_pledge__isnull=True) & Q(funding_return__isnull=True) | Q(release__isnull=True) & Q(auction__isnull=True) & Q(renewal__isnull=False) & Q(funding_pledge__isnull=True) & Q(funding_return__isnull=True) | Q(release__isnull=True) & Q(auction__isnull=True) & Q(renewal__isnull=True) & Q(funding_pledge__isnull=False) & Q(funding_return__isnull=True) | Q(release__isnull=True) & Q(auction__isnull=True) & Q(renewal__isnull=True) & Q(funding_pledge__isnull=True) & Q(funding_return__isnull=False), name='loans_custody_one_source')`

Methods/properties: save (L1763), clean (L1769), delete (L1860).

## PawnLoanReleaseReversal

[apps/tenant_apps/loans/models/core.py](../../apps/tenant_apps/loans/models/core.py), line 1864. Bases: `WorkspaceOwnedModel`.

| Field | Declaration (type, optionality, relation, deletion, default, precision) |
| --- | --- |
| `release` (L1867) | `models.OneToOneField(PawnLoanRelease, on_delete=models.PROTECT, related_name='reversal')` |
| `loan_event` (L1872) | `models.OneToOneField(PawnLoanEvent, on_delete=models.PROTECT, related_name='release_reversal')` |
| `catch_up_reversal_event` (L1877) | `models.OneToOneField(PawnLoanEvent, null=True, blank=True, on_delete=models.PROTECT, related_name='release_catch_up_reversal')` |
| `reason` (L1884) | `models.TextField()` |
| `created_by` (L1885) | `models.ForeignKey(settings.AUTH_USER_MODEL, null=True, blank=True, on_delete=models.SET_NULL, related_name='pawn_loan_release_reversals_created')` |
| `created_at` (L1892) | `models.DateTimeField(auto_now_add=True)` |

Methods/properties: save (L1894), clean (L1900), delete (L1926).

## PawnLoanNotice

[apps/tenant_apps/loans/models/core.py](../../apps/tenant_apps/loans/models/core.py), line 1930. Bases: `models.Model`. ordering=('-created_at', '-id')

| Field | Declaration (type, optionality, relation, deletion, default, precision) |
| --- | --- |
| `workspace` (L1933) | `models.ForeignKey('orgs.Company', on_delete=models.PROTECT, related_name='pawn_loan_notices')` |
| `loan` (L1938) | `models.ForeignKey(PawnLoan, on_delete=models.PROTECT, related_name='notices')` |
| `notice_kind` (L1943) | `models.CharField(max_length=32, choices=enum_choices(PawnLoanNoticeKind))` |
| `channel` (L1947) | `models.CharField(max_length=16, choices=enum_choices(PawnLoanNoticeChannel))` |
| `request_key` (L1951) | `models.CharField(max_length=120)` |
| `scheduled_for` (L1952) | `models.DateTimeField(db_index=True)` |
| `recipient_name` (L1953) | `models.CharField(max_length=255)` |
| `recipient_email` (L1954) | `models.EmailField(blank=True)` |
| `recipient_phone` (L1955) | `models.CharField(max_length=32, blank=True)` |
| `payload_snapshot` (L1956) | `models.JSONField(default=dict)` |
| `notification_event_id` (L1957) | `models.PositiveBigIntegerField(null=True, blank=True)` |
| `notification_job_id` (L1958) | `models.PositiveBigIntegerField(null=True, blank=True)` |
| `source_auction` (L1959) | `models.OneToOneField('PawnLoanAuction', null=True, blank=True, on_delete=models.PROTECT, related_name='notice')` |
| `source_risk_alert` (L1966) | `models.ForeignKey('loans.LoanRiskAlert', null=True, blank=True, on_delete=models.PROTECT, related_name='borrower_notices')` |
| `source_risk_event` (L1970) | `models.ForeignKey('loans.LoanRiskEvent', null=True, blank=True, on_delete=models.PROTECT, related_name='borrower_notices')` |
| `notification_template_id` (L1974) | `models.PositiveBigIntegerField(null=True, blank=True)` |
| `notification_template_version` (L1975) | `models.PositiveIntegerField(null=True, blank=True)` |
| `notification_template_locale` (L1976) | `models.CharField(max_length=10, blank=True)` |
| `created_by` (L1977) | `models.ForeignKey(settings.AUTH_USER_MODEL, null=True, blank=True, on_delete=models.SET_NULL, related_name='pawn_loan_notices_created')` |
| `created_at` (L1984) | `models.DateTimeField(auto_now_add=True)` |
| `updated_at` (L1985) | `models.DateTimeField(auto_now=True)` |

Named constraints:

- L1990: `models.UniqueConstraint(fields=('loan', 'request_key'), name='loans_notice_loan_request_uniq')`
- L1994: `models.UniqueConstraint(fields=('source_risk_event', 'notice_kind', 'channel', 'notification_template_version'), condition=Q(source_risk_event__isnull=False), name='loans_risk_notice_intent_uniq')`

Methods/properties: clean (L2011), save (L2046), delete (L2050).

## PawnLoanAuction

[apps/tenant_apps/loans/models/core.py](../../apps/tenant_apps/loans/models/core.py), line 2054. Bases: `models.Model`. ordering=('loan_id', 'attempt_number')

| Field | Declaration (type, optionality, relation, deletion, default, precision) |
| --- | --- |
| `workspace` (L2057) | `models.ForeignKey('orgs.Company', on_delete=models.PROTECT, related_name='pawn_loan_auctions')` |
| `loan` (L2060) | `models.ForeignKey(PawnLoan, on_delete=models.PROTECT, related_name='auctions')` |
| `auction_number` (L2061) | `models.CharField(max_length=96)` |
| `attempt_number` (L2062) | `models.PositiveIntegerField()` |
| `request_key` (L2063) | `models.CharField(max_length=120)` |
| `state` (L2064) | `models.CharField(max_length=20, choices=enum_choices(PawnLoanAuctionState), default=PawnLoanAuctionState.INITIATED.value, db_index=True)` |
| `notice_date` (L2070) | `models.DateField()` |
| `scheduled_date` (L2071) | `models.DateField(db_index=True)` |
| `started_at` (L2072) | `models.DateTimeField(null=True, blank=True)` |
| `completed_at` (L2073) | `models.DateTimeField(null=True, blank=True)` |
| `cancelled_at` (L2074) | `models.DateTimeField(null=True, blank=True)` |
| `cancellation_reason` (L2075) | `models.TextField(blank=True)` |
| `buyer_name` (L2076) | `models.CharField(max_length=255, blank=True)` |
| `buyer_reference` (L2077) | `models.CharField(max_length=120, blank=True)` |
| `recovery_amount` (L2078) | `models.DecimalField(max_digits=18, decimal_places=4, null=True, blank=True)` |
| `principal_amount` (L2079) | `models.DecimalField(max_digits=18, decimal_places=4, null=True, blank=True)` |
| `interest_amount` (L2080) | `models.DecimalField(max_digits=18, decimal_places=4, null=True, blank=True)` |
| `fee_amount` (L2081) | `models.DecimalField(max_digits=18, decimal_places=4, null=True, blank=True)` |
| `loan_event` (L2082) | `models.OneToOneField(PawnLoanEvent, null=True, blank=True, on_delete=models.PROTECT, related_name='auction')` |
| `catch_up_accrual` (L2089) | `models.OneToOneField(PawnLoanInterestAccrual, null=True, blank=True, on_delete=models.PROTECT, related_name='auction_catch_up')` |
| `created_by` (L2096) | `models.ForeignKey(settings.AUTH_USER_MODEL, null=True, blank=True, on_delete=models.SET_NULL, related_name='pawn_loan_auctions_created')` |
| `created_at` (L2103) | `models.DateTimeField(auto_now_add=True)` |
| `updated_at` (L2104) | `models.DateTimeField(auto_now=True)` |

Named constraints:

- L2109: `models.UniqueConstraint(fields=('workspace', 'auction_number'), name='loans_auction_workspace_number_uniq')`
- L2110: `models.UniqueConstraint(fields=('loan', 'attempt_number'), name='loans_auction_loan_attempt_uniq')`
- L2111: `models.UniqueConstraint(fields=('loan', 'request_key'), name='loans_auction_loan_request_uniq')`

Methods/properties: clean (L2117), save (L2146), delete (L2150).

## PawnLoanAuctionItem

[apps/tenant_apps/loans/models/core.py](../../apps/tenant_apps/loans/models/core.py), line 2154. Bases: `WorkspaceOwnedModel`. ordering=('auction_id', 'id')

| Field | Declaration (type, optionality, relation, deletion, default, precision) |
| --- | --- |
| `auction` (L2155) | `models.ForeignKey(PawnLoanAuction, on_delete=models.PROTECT, related_name='items')` |
| `collateral_item` (L2156) | `models.ForeignKey(PawnCollateralItem, on_delete=models.PROTECT, related_name='auction_items')` |
| `snapshot` (L2157) | `models.JSONField(default=dict)` |
| `disposed_at` (L2158) | `models.DateTimeField(default=timezone.now)` |

Named constraints:

- L2162: `models.UniqueConstraint(fields=('auction', 'collateral_item'), name='loans_auction_item_uniq')`

Methods/properties: save (L2164), delete (L2171).

## PawnLoanAuctionReversal

[apps/tenant_apps/loans/models/core.py](../../apps/tenant_apps/loans/models/core.py), line 2175. Bases: `WorkspaceOwnedModel`.

| Field | Declaration (type, optionality, relation, deletion, default, precision) |
| --- | --- |
| `auction` (L2176) | `models.OneToOneField(PawnLoanAuction, on_delete=models.PROTECT, related_name='reversal')` |
| `loan_event` (L2177) | `models.OneToOneField(PawnLoanEvent, on_delete=models.PROTECT, related_name='auction_reversal')` |
| `catch_up_reversal_event` (L2178) | `models.OneToOneField(PawnLoanEvent, null=True, blank=True, on_delete=models.PROTECT, related_name='auction_catch_up_reversal')` |
| `reason` (L2185) | `models.TextField()` |
| `created_by` (L2186) | `models.ForeignKey(settings.AUTH_USER_MODEL, null=True, blank=True, on_delete=models.SET_NULL, related_name='pawn_loan_auction_reversals_created')` |
| `created_at` (L2193) | `models.DateTimeField(auto_now_add=True)` |

Methods/properties: save (L2195), delete (L2204).

## PawnLoanRenewal

[apps/tenant_apps/loans/models/core.py](../../apps/tenant_apps/loans/models/core.py), line 2208. Bases: `models.Model`. ordering=('-renewal_date', '-id')

| Field | Declaration (type, optionality, relation, deletion, default, precision) |
| --- | --- |
| `workspace` (L2211) | `models.ForeignKey('orgs.Company', on_delete=models.PROTECT, related_name='pawn_loan_renewals')` |
| `source_loan` (L2214) | `models.OneToOneField(PawnLoan, on_delete=models.PROTECT, related_name='renewal_as_source')` |
| `successor_loan` (L2217) | `models.OneToOneField(PawnLoan, on_delete=models.PROTECT, related_name='origin_renewal')` |
| `renewal_number` (L2220) | `models.CharField(max_length=96)` |
| `request_key` (L2221) | `models.CharField(max_length=120)` |
| `mode` (L2222) | `models.CharField(max_length=20, choices=enum_choices(PawnLoanRenewalMode))` |
| `renewal_date` (L2223) | `models.DateField(db_index=True)` |
| `source_principal_amount` (L2224) | `models.DecimalField(max_digits=18, decimal_places=4)` |
| `source_capitalized_principal_amount` (L2225) | `models.DecimalField(max_digits=18, decimal_places=4)` |
| `interest_settled` (L2226) | `models.DecimalField(max_digits=18, decimal_places=4)` |
| `fees_settled` (L2227) | `models.DecimalField(max_digits=18, decimal_places=4)` |
| `principal_paid` (L2228) | `models.DecimalField(max_digits=18, decimal_places=4)` |
| `top_up_amount` (L2229) | `models.DecimalField(max_digits=18, decimal_places=4)` |
| `successor_principal_amount` (L2230) | `models.DecimalField(max_digits=18, decimal_places=4)` |
| `successor_capitalized_principal_amount` (L2231) | `models.DecimalField(max_digits=18, decimal_places=4)` |
| `successor_advance_interest` (L2232) | `models.DecimalField(max_digits=18, decimal_places=4, default=Decimal('0'))` |
| `successor_deducted_fees` (L2237) | `models.DecimalField(max_digits=18, decimal_places=4, default=Decimal('0'))` |
| `valuation_snapshot` (L2242) | `models.JSONField(default=dict)` |
| `settlement_event` (L2243) | `models.OneToOneField(PawnLoanEvent, on_delete=models.PROTECT, related_name='renewal_settlement')` |
| `opening_event` (L2248) | `models.OneToOneField(PawnLoanEvent, on_delete=models.PROTECT, related_name='renewal_opening')` |
| `catch_up_accrual` (L2253) | `models.OneToOneField(PawnLoanInterestAccrual, null=True, blank=True, on_delete=models.PROTECT, related_name='renewal_catch_up')` |
| `created_by` (L2260) | `models.ForeignKey(settings.AUTH_USER_MODEL, null=True, blank=True, on_delete=models.SET_NULL, related_name='pawn_loan_renewals_created')` |
| `created_at` (L2267) | `models.DateTimeField(auto_now_add=True)` |

Named constraints:

- L2272: `models.UniqueConstraint(fields=('workspace', 'renewal_number'), name='loans_renewal_workspace_number_uniq')`
- L2276: `models.UniqueConstraint(fields=('workspace', 'request_key'), name='loans_renewal_workspace_request_uniq')`

Methods/properties: clean (L2282), save (L2317), delete (L2323).

## PawnLoanRenewalReversal

[apps/tenant_apps/loans/models/core.py](../../apps/tenant_apps/loans/models/core.py), line 2327. Bases: `WorkspaceOwnedModel`.

| Field | Declaration (type, optionality, relation, deletion, default, precision) |
| --- | --- |
| `renewal` (L2328) | `models.OneToOneField(PawnLoanRenewal, on_delete=models.PROTECT, related_name='reversal')` |
| `settlement_reversal_event` (L2331) | `models.OneToOneField(PawnLoanEvent, on_delete=models.PROTECT, related_name='renewal_settlement_reversal')` |
| `opening_reversal_event` (L2336) | `models.OneToOneField(PawnLoanEvent, on_delete=models.PROTECT, related_name='renewal_opening_reversal')` |
| `catch_up_reversal_event` (L2341) | `models.OneToOneField(PawnLoanEvent, null=True, blank=True, on_delete=models.PROTECT, related_name='renewal_catch_up_reversal')` |
| `reason` (L2348) | `models.TextField()` |
| `created_by` (L2349) | `models.ForeignKey(settings.AUTH_USER_MODEL, null=True, blank=True, on_delete=models.SET_NULL, related_name='pawn_loan_renewal_reversals_created')` |
| `created_at` (L2356) | `models.DateTimeField(auto_now_add=True)` |

Methods/properties: save (L2358), delete (L2371).

## LoanDocumentLayout

[apps/tenant_apps/loans/models/documents.py](../../apps/tenant_apps/loans/models/documents.py), line 32. Bases: `models.Model`. ordering=('document_type', 'name', 'pk')

| Field | Declaration (type, optionality, relation, deletion, default, precision) |
| --- | --- |
| `workspace` (L33) | `models.ForeignKey('orgs.Company', on_delete=models.PROTECT, related_name='loan_document_layouts')` |
| `document_type` (L34) | `models.CharField(max_length=32, choices=DOCUMENT_KIND_CHOICES)` |
| `name` (L35) | `models.CharField(max_length=100)` |
| `is_retired` (L36) | `models.BooleanField(default=False, db_index=True)` |
| `created_at` (L37) | `models.DateTimeField(auto_now_add=True)` |
| `created_by` (L38) | `models.ForeignKey(settings.AUTH_USER_MODEL, null=True, on_delete=models.SET_NULL, related_name='loan_document_layouts_created')` |

Named constraints:

- L42: `models.UniqueConstraint(fields=('workspace', 'document_type', 'name'), name='loans_doc_layout_name_uniq')`

Methods/properties: clean (L44), save (L49).

## LoanDocumentLayoutRevision

[apps/tenant_apps/loans/models/documents.py](../../apps/tenant_apps/loans/models/documents.py), line 54. Bases: `WorkspaceOwnedModel`. ordering=('layout_id', 'version')

| Field | Declaration (type, optionality, relation, deletion, default, precision) |
| --- | --- |
| `layout` (L60) | `models.ForeignKey(LoanDocumentLayout, on_delete=models.PROTECT, related_name='revisions')` |
| `version` (L61) | `models.PositiveIntegerField()` |
| `state` (L62) | `models.CharField(max_length=12, choices=State.choices, default=State.DRAFT, db_index=True)` |
| `definition` (L63) | `models.JSONField(default=dict)` |
| `content_hash` (L64) | `models.CharField(max_length=64, blank=True)` |
| `validation_result` (L65) | `models.JSONField(default=dict, blank=True)` |
| `validated_at` (L66) | `models.DateTimeField(null=True, blank=True)` |
| `published_at` (L67) | `models.DateTimeField(null=True, blank=True)` |
| `retired_at` (L68) | `models.DateTimeField(null=True, blank=True)` |
| `created_at` (L69) | `models.DateTimeField(auto_now_add=True)` |
| `created_by` (L70) | `models.ForeignKey(settings.AUTH_USER_MODEL, null=True, on_delete=models.SET_NULL, related_name='loan_document_revisions_created')` |

Named constraints:

- L75: `models.UniqueConstraint(fields=('layout', 'version'), name='loans_doc_revision_version_uniq')`
- L76: `models.CheckConstraint(condition=Q(version__gt=0), name='loans_doc_revision_version_positive')`

Methods/properties: clean (L79), save (L91).

## LoanDocumentAsset

[apps/tenant_apps/loans/models/documents.py](../../apps/tenant_apps/loans/models/documents.py), line 96. Bases: `models.Model`.

| Field | Declaration (type, optionality, relation, deletion, default, precision) |
| --- | --- |
| `revision` (L97) | `models.ForeignKey(LoanDocumentLayoutRevision, on_delete=models.PROTECT, related_name='assets')` |
| `workspace` (L98) | `models.ForeignKey('orgs.Company', on_delete=models.PROTECT, related_name='loan_document_assets')` |
| `key` (L99) | `models.CharField(max_length=64)` |
| `kind` (L100) | `models.CharField(max_length=16, choices=(('IMAGE', 'Image'), ('BACKGROUND', 'Background')))` |
| `mime_type` (L101) | `models.CharField(max_length=64)` |
| `file` (L102) | `models.FileField(upload_to=document_asset_upload)` |
| `sha256` (L103) | `models.CharField(max_length=64)` |
| `byte_size` (L104) | `models.PositiveIntegerField()` |
| `width` (L105) | `models.PositiveIntegerField(null=True, blank=True)` |
| `height` (L106) | `models.PositiveIntegerField(null=True, blank=True)` |
| `page_count` (L107) | `models.PositiveSmallIntegerField(null=True, blank=True)` |
| `created_at` (L108) | `models.DateTimeField(auto_now_add=True)` |
| `created_by` (L109) | `models.ForeignKey(settings.AUTH_USER_MODEL, null=True, on_delete=models.SET_NULL, related_name='loan_document_assets_created')` |

Named constraints:

- L112: `models.UniqueConstraint(fields=('revision', 'key'), name='loans_doc_asset_key_uniq')`

Methods/properties: clean (L114), save (L128).

## LoanDocumentLayoutAssignment

[apps/tenant_apps/loans/models/documents.py](../../apps/tenant_apps/loans/models/documents.py), line 133. Bases: `models.Model`.

| Field | Declaration (type, optionality, relation, deletion, default, precision) |
| --- | --- |
| `workspace` (L134) | `models.ForeignKey('orgs.Company', on_delete=models.PROTECT, related_name='loan_document_assignments')` |
| `document_type` (L135) | `models.CharField(max_length=32, choices=DOCUMENT_KIND_CHOICES)` |
| `revision` (L136) | `models.ForeignKey(LoanDocumentLayoutRevision, on_delete=models.PROTECT, related_name='assignments')` |
| `license` (L137) | `models.ForeignKey(LoanLicense, null=True, blank=True, on_delete=models.PROTECT, related_name='document_layout_assignments')` |
| `series` (L138) | `models.ForeignKey(LoanSeries, null=True, blank=True, on_delete=models.PROTECT, related_name='document_layout_assignments')` |
| `is_active` (L139) | `models.BooleanField(default=True, db_index=True)` |
| `created_at` (L140) | `models.DateTimeField(auto_now_add=True)` |
| `created_by` (L141) | `models.ForeignKey(settings.AUTH_USER_MODEL, null=True, on_delete=models.SET_NULL, related_name='loan_document_assignments_created')` |

Named constraints:

- L145: `models.UniqueConstraint(fields=('workspace', 'document_type'), condition=Q(is_active=True, license__isnull=True, series__isnull=True), name='loans_doc_assign_workspace_uniq')`
- L146: `models.UniqueConstraint(fields=('workspace', 'document_type', 'license'), condition=Q(is_active=True, license__isnull=False, series__isnull=True), name='loans_doc_assign_license_uniq')`
- L147: `models.UniqueConstraint(fields=('workspace', 'document_type', 'series'), condition=Q(is_active=True, series__isnull=False), name='loans_doc_assign_series_uniq')`

Methods/properties: clean (L150), save (L170).

## LoanDocumentPrintProfile

[apps/tenant_apps/loans/models/documents.py](../../apps/tenant_apps/loans/models/documents.py), line 175. Bases: `models.Model`. ordering=('document_type', 'name', 'pk')

| Field | Declaration (type, optionality, relation, deletion, default, precision) |
| --- | --- |
| `workspace` (L176) | `models.ForeignKey('orgs.Company', on_delete=models.PROTECT, related_name='loan_document_print_profiles')` |
| `document_type` (L180) | `models.CharField(max_length=32, choices=DOCUMENT_KIND_CHOICES)` |
| `name` (L181) | `models.CharField(max_length=100)` |
| `is_retired` (L182) | `models.BooleanField(default=False, db_index=True)` |
| `created_at` (L183) | `models.DateTimeField(auto_now_add=True)` |
| `created_by` (L184) | `models.ForeignKey(settings.AUTH_USER_MODEL, null=True, on_delete=models.SET_NULL, related_name='loan_document_print_profiles_created')` |

Named constraints:

- L192: `models.UniqueConstraint(fields=('workspace', 'document_type', 'name'), name='loans_print_profile_name_uniq')`

Methods/properties: clean (L198), save (L205).

## LoanDocumentPrintProfileRevision

[apps/tenant_apps/loans/models/documents.py](../../apps/tenant_apps/loans/models/documents.py), line 210. Bases: `WorkspaceOwnedModel`. ordering=('profile_id', 'version')

| Field | Declaration (type, optionality, relation, deletion, default, precision) |
| --- | --- |
| `profile` (L216) | `models.ForeignKey(LoanDocumentPrintProfile, on_delete=models.PROTECT, related_name='revisions')` |
| `version` (L219) | `models.PositiveIntegerField()` |
| `state` (L220) | `models.CharField(max_length=12, choices=State.choices, default=State.DRAFT, db_index=True)` |
| `definition` (L223) | `models.JSONField(default=dict)` |
| `content_hash` (L224) | `models.CharField(max_length=64, blank=True)` |
| `validation_result` (L225) | `models.JSONField(default=dict, blank=True)` |
| `validated_at` (L226) | `models.DateTimeField(null=True, blank=True)` |
| `published_at` (L227) | `models.DateTimeField(null=True, blank=True)` |
| `retired_at` (L228) | `models.DateTimeField(null=True, blank=True)` |
| `created_at` (L229) | `models.DateTimeField(auto_now_add=True)` |
| `created_by` (L230) | `models.ForeignKey(settings.AUTH_USER_MODEL, null=True, on_delete=models.SET_NULL, related_name='loan_document_print_profile_revisions_created')` |

Named constraints:

- L238: `models.UniqueConstraint(fields=('profile', 'version'), name='loans_print_rev_version_uniq')`
- L241: `models.CheckConstraint(condition=Q(version__gt=0), name='loans_print_rev_version_pos')`

Methods/properties: clean (L246), save (L266).

## LoanDocumentPrintProfileAssignment

[apps/tenant_apps/loans/models/documents.py](../../apps/tenant_apps/loans/models/documents.py), line 271. Bases: `models.Model`.

| Field | Declaration (type, optionality, relation, deletion, default, precision) |
| --- | --- |
| `workspace` (L272) | `models.ForeignKey('orgs.Company', on_delete=models.PROTECT, related_name='loan_document_print_profile_assignments')` |
| `document_type` (L276) | `models.CharField(max_length=32, choices=DOCUMENT_KIND_CHOICES)` |
| `revision` (L277) | `models.ForeignKey(LoanDocumentPrintProfileRevision, on_delete=models.PROTECT, related_name='assignments')` |
| `series` (L282) | `models.ForeignKey(LoanSeries, null=True, blank=True, on_delete=models.PROTECT, related_name='document_print_profile_assignments')` |
| `is_active` (L286) | `models.BooleanField(default=True, db_index=True)` |
| `created_at` (L287) | `models.DateTimeField(auto_now_add=True)` |
| `created_by` (L288) | `models.ForeignKey(settings.AUTH_USER_MODEL, null=True, on_delete=models.SET_NULL, related_name='loan_document_print_profile_assignments_created')` |

Named constraints:

- L295: `models.UniqueConstraint(fields=('workspace', 'document_type'), condition=Q(is_active=True, series__isnull=True), name='loans_print_assign_ws_uniq')`
- L300: `models.UniqueConstraint(fields=('workspace', 'document_type', 'series'), condition=Q(is_active=True, series__isnull=False), name='loans_print_assign_series_uniq')`

Methods/properties: clean (L307), save (L325).

## LoanDocumentIssue

[apps/tenant_apps/loans/models/documents.py](../../apps/tenant_apps/loans/models/documents.py), line 330. Bases: `models.Model`. ordering=('-issued_at', '-pk')

| Field | Declaration (type, optionality, relation, deletion, default, precision) |
| --- | --- |
| `workspace` (L341) | `models.ForeignKey('orgs.Company', on_delete=models.PROTECT, related_name='loan_document_issues')` |
| `document_type` (L342) | `models.CharField(max_length=32, choices=DOCUMENT_KIND_CHOICES)` |
| `issue_kind` (L343) | `models.CharField(max_length=12, choices=Kind.choices, default=Kind.OFFICIAL)` |
| `source_type` (L344) | `models.CharField(max_length=64)` |
| `source_id` (L345) | `models.CharField(max_length=64)` |
| `source_fingerprint` (L346) | `models.CharField(max_length=128)` |
| `revision` (L347) | `models.ForeignKey(LoanDocumentLayoutRevision, null=True, blank=True, on_delete=models.PROTECT, related_name='issues')` |
| `print_profile_revision` (L348) | `models.ForeignKey(LoanDocumentPrintProfileRevision, null=True, blank=True, on_delete=models.PROTECT, related_name='issues')` |
| `print_profile_name` (L352) | `models.CharField(max_length=100, blank=True)` |
| `print_profile_version` (L353) | `models.PositiveIntegerField(null=True, blank=True)` |
| `print_profile_hash` (L354) | `models.CharField(max_length=64, blank=True)` |
| `print_profile_source_scope` (L355) | `models.CharField(max_length=16, choices=PrintProfileSource.choices, blank=True)` |
| `fixed_renderer_version` (L358) | `models.CharField(max_length=64, blank=True)` |
| `payload_schema_version` (L359) | `models.PositiveIntegerField()` |
| `payload_hash` (L360) | `models.CharField(max_length=64)` |
| `layout_hash` (L361) | `models.CharField(max_length=64, blank=True)` |
| `asset_hashes` (L362) | `models.JSONField(default=dict, blank=True)` |
| `pdf_hash` (L363) | `models.CharField(max_length=64)` |
| `artifact` (L364) | `models.FileField(upload_to=document_issue_upload)` |
| `prior_issue` (L365) | `models.ForeignKey('self', null=True, blank=True, on_delete=models.PROTECT, related_name='regenerated_issues')` |
| `issued_at` (L366) | `models.DateTimeField(auto_now_add=True)` |
| `issued_by` (L367) | `models.ForeignKey(settings.AUTH_USER_MODEL, null=True, on_delete=models.SET_NULL, related_name='loan_document_issues_created')` |

Named constraints:

- L373: `models.UniqueConstraint(fields=('workspace', 'document_type', 'source_type', 'source_id', 'source_fingerprint'), condition=Q(issue_kind='OFFICIAL'), name='loans_doc_official_issue_uniq')`

Methods/properties: clean (L380), save (L434).

## ImmutableEvidenceModel

[apps/tenant_apps/loans/models/funding.py](../../apps/tenant_apps/loans/models/funding.py), line 21. Bases: `WorkspaceOwnedModel`. abstract=True

| Field | Declaration (type, optionality, relation, deletion, default, precision) |
| --- | --- |

Methods/properties: save (L25), delete (L31).

## FundingLoanSequence

[apps/tenant_apps/loans/models/funding.py](../../apps/tenant_apps/loans/models/funding.py), line 35. Bases: `models.Model`.

| Field | Declaration (type, optionality, relation, deletion, default, precision) |
| --- | --- |
| `workspace` (L36) | `models.OneToOneField('orgs.Company', on_delete=models.PROTECT, related_name='funding_loan_sequence')` |
| `prefix` (L41) | `models.CharField(max_length=24, default='FL-')` |
| `width` (L42) | `models.PositiveSmallIntegerField(default=6)` |
| `next_value` (L43) | `models.PositiveBigIntegerField(default=1)` |
| `maximum_value` (L44) | `models.PositiveBigIntegerField(null=True, blank=True)` |
| `updated_at` (L45) | `models.DateTimeField(auto_now=True)` |

Named constraints:

- L49: `models.CheckConstraint(condition=Q(width__gt=0) & Q(width__lte=18), name='loans_funding_seq_width_valid')`
- L53: `models.CheckConstraint(condition=Q(next_value__gt=0), name='loans_funding_seq_next_positive')`
- L57: `models.CheckConstraint(condition=Q(maximum_value__isnull=True) | Q(next_value__lte=F('maximum_value') + 1), name='loans_funding_seq_max_valid')`

Methods/properties: none declared; inherited behavior still applies.

## FundingLoan

[apps/tenant_apps/loans/models/funding.py](../../apps/tenant_apps/loans/models/funding.py), line 65. Bases: `models.Model`. ordering=('created_at', 'funding_number')

| Field | Declaration (type, optionality, relation, deletion, default, precision) |
| --- | --- |
| `workspace` (L66) | `models.ForeignKey('orgs.Company', on_delete=models.PROTECT, related_name='funding_loans')` |
| `lender` (L71) | `models.ForeignKey('party.Party', on_delete=models.PROTECT, related_name='funding_loans')` |
| `funding_number` (L76) | `models.CharField(max_length=64)` |
| `state` (L77) | `models.CharField(max_length=24, choices=enum_choices(FundingLoanState), default=FundingLoanState.DRAFT.value, db_index=True)` |
| `created_at` (L83) | `models.DateTimeField(auto_now_add=True)` |
| `updated_at` (L84) | `models.DateTimeField(auto_now=True)` |
| `created_by` (L85) | `models.ForeignKey(settings.AUTH_USER_MODEL, null=True, blank=True, on_delete=models.SET_NULL, related_name='funding_loans_created')` |
| `updated_by` (L92) | `models.ForeignKey(settings.AUTH_USER_MODEL, null=True, blank=True, on_delete=models.SET_NULL, related_name='funding_loans_updated')` |

Named constraints:

- L103: `models.CheckConstraint(condition=Q(state__in=tuple((state.value for state in FundingLoanState))), name='loans_funding_state_valid')`
- L109: `models.UniqueConstraint(fields=('workspace', 'funding_number'), name='loans_funding_workspace_number_uniq')`

Methods/properties: clean (L125), save (L133).

## FundingLoanDraftTerms

[apps/tenant_apps/loans/models/funding.py](../../apps/tenant_apps/loans/models/funding.py), line 138. Bases: `WorkspaceOwnedModel`.

| Field | Declaration (type, optionality, relation, deletion, default, precision) |
| --- | --- |
| `funding_loan` (L139) | `models.OneToOneField(FundingLoan, on_delete=models.CASCADE, related_name='draft_terms')` |
| `principal_amount` (L144) | `models.DecimalField(max_digits=18, decimal_places=4)` |
| `monthly_interest_rate` (L145) | `models.DecimalField(max_digits=9, decimal_places=6)` |
| `activated_on` (L146) | `models.DateField()` |
| `maturity_on` (L147) | `models.DateField()` |
| `maximum_funding_ltv_ratio` (L148) | `models.DecimalField(max_digits=7, decimal_places=6, default=Decimal('0.800000'))` |
| `currency_quantum` (L153) | `models.DecimalField(max_digits=8, decimal_places=4, default=Decimal('0.0100'))` |
| `updated_by` (L158) | `models.ForeignKey(settings.AUTH_USER_MODEL, null=True, blank=True, on_delete=models.SET_NULL, related_name='funding_draft_terms_updated')` |
| `updated_at` (L165) | `models.DateTimeField(auto_now=True)` |

Named constraints:

- L169: `models.CheckConstraint(condition=Q(principal_amount__gt=0) & Q(monthly_interest_rate__gte=0) & Q(maturity_on__gte=F('activated_on')) & Q(maximum_funding_ltv_ratio__gt=0) & Q(maximum_funding_ltv_ratio__lte=1) & Q(currency_quantum__gt=0), name='loans_funding_draft_terms_valid')`

Methods/properties: none declared; inherited behavior still applies.

## FundingLoanDraftCollateral

[apps/tenant_apps/loans/models/funding.py](../../apps/tenant_apps/loans/models/funding.py), line 181. Bases: `WorkspaceOwnedModel`. ordering=('collateral_item__loan_id', 'collateral_item_id')

| Field | Declaration (type, optionality, relation, deletion, default, precision) |
| --- | --- |
| `funding_loan` (L182) | `models.ForeignKey(FundingLoan, on_delete=models.CASCADE, related_name='draft_collateral')` |
| `collateral_item` (L187) | `models.ForeignKey(PawnCollateralItem, on_delete=models.PROTECT, related_name='funding_draft_selections')` |
| `selected_collateral_value` (L192) | `models.DecimalField(max_digits=18, decimal_places=4)` |
| `created_at` (L193) | `models.DateTimeField(auto_now_add=True)` |

Named constraints:

- L198: `models.UniqueConstraint(fields=('funding_loan', 'collateral_item'), name='loans_funding_draft_item_uniq')`
- L202: `models.CheckConstraint(condition=Q(selected_collateral_value__gt=0), name='loans_funding_draft_item_value_positive')`

Methods/properties: none declared; inherited behavior still applies.

## FundingLoanCancellation

[apps/tenant_apps/loans/models/funding.py](../../apps/tenant_apps/loans/models/funding.py), line 209. Bases: `ImmutableEvidenceModel`.

| Field | Declaration (type, optionality, relation, deletion, default, precision) |
| --- | --- |
| `funding_loan` (L210) | `models.OneToOneField(FundingLoan, on_delete=models.PROTECT, related_name='cancellation')` |
| `reason` (L215) | `models.TextField()` |
| `actor` (L216) | `models.ForeignKey(settings.AUTH_USER_MODEL, null=True, blank=True, on_delete=models.SET_NULL, related_name='funding_loan_cancellations')` |
| `created_at` (L223) | `models.DateTimeField(auto_now_add=True)` |

Methods/properties: clean (L225).

## FundingLoanTermsSnapshot

[apps/tenant_apps/loans/models/funding.py](../../apps/tenant_apps/loans/models/funding.py), line 231. Bases: `ImmutableEvidenceModel`.

| Field | Declaration (type, optionality, relation, deletion, default, precision) |
| --- | --- |
| `funding_loan` (L232) | `models.OneToOneField(FundingLoan, on_delete=models.PROTECT, related_name='terms_snapshot')` |
| `principal_amount` (L237) | `models.DecimalField(max_digits=18, decimal_places=4)` |
| `monthly_interest_rate` (L238) | `models.DecimalField(max_digits=9, decimal_places=6)` |
| `activated_on` (L239) | `models.DateField(db_index=True)` |
| `maturity_on` (L240) | `models.DateField(db_index=True)` |
| `maximum_funding_ltv_ratio` (L241) | `models.DecimalField(max_digits=7, decimal_places=6, default=Decimal('0.800000'))` |
| `currency_quantum` (L246) | `models.DecimalField(max_digits=8, decimal_places=4, default=Decimal('0.0100'))` |
| `schema_version` (L251) | `models.PositiveSmallIntegerField(default=1)` |
| `fingerprint` (L252) | `models.CharField(max_length=64)` |
| `created_by` (L253) | `models.ForeignKey(settings.AUTH_USER_MODEL, null=True, blank=True, on_delete=models.SET_NULL, related_name='funding_terms_created')` |
| `created_at` (L260) | `models.DateTimeField(auto_now_add=True)` |

Named constraints:

- L264: `models.CheckConstraint(condition=Q(principal_amount__gt=0) & Q(monthly_interest_rate__gte=0) & Q(maturity_on__gte=F('activated_on')) & Q(maximum_funding_ltv_ratio__gt=0) & Q(maximum_funding_ltv_ratio__lte=1) & Q(currency_quantum__gt=0) & Q(schema_version__gt=0), name='loans_funding_terms_valid')`

Methods/properties: none declared; inherited behavior still applies.

## FundingLoanEvent

[apps/tenant_apps/loans/models/funding.py](../../apps/tenant_apps/loans/models/funding.py), line 277. Bases: `ImmutableEvidenceModel`. ordering=('funding_loan_id', 'sequence')

| Field | Declaration (type, optionality, relation, deletion, default, precision) |
| --- | --- |
| `funding_loan` (L278) | `models.ForeignKey(FundingLoan, on_delete=models.PROTECT, related_name='events')` |
| `sequence` (L283) | `models.PositiveIntegerField()` |
| `event_kind` (L284) | `models.CharField(max_length=24, choices=enum_choices(FundingLoanEventKind))` |
| `operation` (L288) | `models.CharField(max_length=40)` |
| `effective_date` (L289) | `models.DateField(db_index=True)` |
| `principal_amount` (L290) | `models.DecimalField(max_digits=18, decimal_places=4, default=Decimal('0'))` |
| `interest_amount` (L295) | `models.DecimalField(max_digits=18, decimal_places=4, default=Decimal('0'))` |
| `fee_amount` (L300) | `models.DecimalField(max_digits=18, decimal_places=4, default=Decimal('0'))` |
| `request_key` (L305) | `models.CharField(max_length=120)` |
| `request_fingerprint` (L306) | `models.CharField(max_length=64)` |
| `reversal_of` (L307) | `models.OneToOneField('self', null=True, blank=True, on_delete=models.PROTECT, related_name='reversed_by')` |
| `reason` (L314) | `models.TextField(blank=True)` |
| `actor` (L315) | `models.ForeignKey(settings.AUTH_USER_MODEL, null=True, blank=True, on_delete=models.SET_NULL, related_name='funding_loan_events')` |
| `created_at` (L322) | `models.DateTimeField(auto_now_add=True)` |

Named constraints:

- L327: `models.UniqueConstraint(fields=('funding_loan', 'sequence'), name='loans_funding_event_sequence_uniq')`
- L331: `models.UniqueConstraint(fields=('funding_loan', 'operation', 'request_key'), name='loans_funding_event_request_uniq')`
- L335: `models.CheckConstraint(condition=Q(sequence__gt=0), name='loans_funding_event_sequence_positive')`
- L339: `models.CheckConstraint(condition=Q(principal_amount__gte=0) & Q(interest_amount__gte=0) & Q(fee_amount__gte=0), name='loans_funding_event_amounts_nonnegative')`
- L345: `models.CheckConstraint(condition=Q(event_kind=FundingLoanEventKind.REVERSAL.value, reversal_of__isnull=False) | ~Q(event_kind=FundingLoanEventKind.REVERSAL.value) & Q(reversal_of__isnull=True), name='loans_funding_event_reversal_link')`

Methods/properties: clean (L370).

## FundingPledge

[apps/tenant_apps/loans/models/funding.py](../../apps/tenant_apps/loans/models/funding.py), line 385. Bases: `ImmutableEvidenceModel`.

| Field | Declaration (type, optionality, relation, deletion, default, precision) |
| --- | --- |
| `workspace` (L386) | `models.ForeignKey('orgs.Company', on_delete=models.PROTECT, related_name='funding_pledges')` |
| `funding_loan` (L391) | `models.OneToOneField(FundingLoan, on_delete=models.PROTECT, related_name='pledge')` |
| `effective_date` (L396) | `models.DateField(db_index=True)` |
| `request_key` (L397) | `models.CharField(max_length=120)` |
| `request_fingerprint` (L398) | `models.CharField(max_length=64)` |
| `total_collateral_value` (L399) | `models.DecimalField(max_digits=18, decimal_places=4)` |
| `maximum_funded_amount` (L400) | `models.DecimalField(max_digits=18, decimal_places=4)` |
| `valuation_method` (L401) | `models.CharField(max_length=48)` |
| `valuation_version` (L402) | `models.PositiveSmallIntegerField(default=1)` |
| `valuation_snapshot` (L403) | `models.JSONField(default=dict)` |
| `actor` (L404) | `models.ForeignKey(settings.AUTH_USER_MODEL, null=True, blank=True, on_delete=models.SET_NULL, related_name='funding_pledges_created')` |
| `created_at` (L411) | `models.DateTimeField(auto_now_add=True)` |

Named constraints:

- L415: `models.UniqueConstraint(fields=('funding_loan', 'request_key'), name='loans_funding_pledge_request_uniq')`
- L419: `models.CheckConstraint(condition=Q(total_collateral_value__gt=0) & Q(maximum_funded_amount__gt=0) & Q(valuation_version__gt=0), name='loans_funding_pledge_values_valid')`

Methods/properties: clean (L427).

## FundingPledgeItem

[apps/tenant_apps/loans/models/funding.py](../../apps/tenant_apps/loans/models/funding.py), line 436. Bases: `WorkspaceOwnedModel`.

| Field | Declaration (type, optionality, relation, deletion, default, precision) |
| --- | --- |
| `funding_pledge` (L437) | `models.ForeignKey(FundingPledge, on_delete=models.PROTECT, related_name='items')` |
| `collateral_item` (L442) | `models.ForeignKey(PawnCollateralItem, on_delete=models.PROTECT, related_name='funding_pledge_items')` |
| `source_pawn_loan_id` (L447) | `models.PositiveBigIntegerField()` |
| `selected_collateral_value` (L448) | `models.DecimalField(max_digits=18, decimal_places=4)` |
| `valuation_snapshot` (L449) | `models.JSONField(default=dict)` |
| `valuation_fingerprint` (L450) | `models.CharField(max_length=64)` |
| `released_at` (L451) | `models.DateTimeField(null=True, blank=True, db_index=True)` |
| `created_at` (L452) | `models.DateTimeField(auto_now_add=True)` |

Named constraints:

- L456: `models.UniqueConstraint(fields=('funding_pledge', 'collateral_item'), name='loans_funding_pledge_item_uniq')`
- L460: `models.UniqueConstraint(fields=('collateral_item',), condition=Q(released_at__isnull=True), name='loans_funding_active_item_uniq')`
- L465: `models.CheckConstraint(condition=Q(selected_collateral_value__gt=0), name='loans_funding_item_value_positive')`

Methods/properties: clean (L471), save (L483), delete (L515).

## FundingReturn

[apps/tenant_apps/loans/models/funding.py](../../apps/tenant_apps/loans/models/funding.py), line 519. Bases: `ImmutableEvidenceModel`.

| Field | Declaration (type, optionality, relation, deletion, default, precision) |
| --- | --- |
| `workspace` (L520) | `models.ForeignKey('orgs.Company', on_delete=models.PROTECT, related_name='funding_returns')` |
| `funding_loan` (L525) | `models.ForeignKey(FundingLoan, on_delete=models.PROTECT, related_name='returns')` |
| `effective_date` (L530) | `models.DateField(db_index=True)` |
| `operation` (L531) | `models.CharField(max_length=40, default='RETURN_COLLATERAL')` |
| `request_key` (L532) | `models.CharField(max_length=120)` |
| `request_fingerprint` (L533) | `models.CharField(max_length=64)` |
| `principal_outstanding` (L534) | `models.DecimalField(max_digits=18, decimal_places=4)` |
| `retained_collateral_value` (L535) | `models.DecimalField(max_digits=18, decimal_places=4)` |
| `retained_ltv_ratio` (L536) | `models.DecimalField(max_digits=9, decimal_places=6, null=True, blank=True)` |
| `evidence_snapshot` (L542) | `models.JSONField(default=dict)` |
| `actor` (L543) | `models.ForeignKey(settings.AUTH_USER_MODEL, null=True, blank=True, on_delete=models.SET_NULL, related_name='funding_returns_created')` |
| `created_at` (L550) | `models.DateTimeField(auto_now_add=True)` |

Named constraints:

- L554: `models.UniqueConstraint(fields=('funding_loan', 'operation', 'request_key'), name='loans_funding_return_request_uniq')`
- L558: `models.CheckConstraint(condition=Q(principal_outstanding__gte=0) & Q(retained_collateral_value__gte=0) & (Q(retained_ltv_ratio__isnull=True) | Q(retained_ltv_ratio__gte=0) & Q(retained_ltv_ratio__lte=1)), name='loans_funding_return_values_valid')`

Methods/properties: clean (L572).

## FundingReturnItem

[apps/tenant_apps/loans/models/funding.py](../../apps/tenant_apps/loans/models/funding.py), line 581. Bases: `ImmutableEvidenceModel`.

| Field | Declaration (type, optionality, relation, deletion, default, precision) |
| --- | --- |
| `funding_return` (L582) | `models.ForeignKey(FundingReturn, on_delete=models.PROTECT, related_name='items')` |
| `pledge_item` (L587) | `models.ForeignKey(FundingPledgeItem, on_delete=models.PROTECT, related_name='return_items')` |
| `returned_at` (L592) | `models.DateTimeField(default=timezone.now)` |
| `evidence_snapshot` (L593) | `models.JSONField(default=dict)` |

Named constraints:

- L597: `models.UniqueConstraint(fields=('funding_return', 'pledge_item'), name='loans_funding_return_item_uniq')`

Methods/properties: clean (L603).

## FundingPledgeReversal

[apps/tenant_apps/loans/models/funding.py](../../apps/tenant_apps/loans/models/funding.py), line 615. Bases: `ImmutableEvidenceModel`.

| Field | Declaration (type, optionality, relation, deletion, default, precision) |
| --- | --- |
| `funding_pledge` (L616) | `models.OneToOneField(FundingPledge, on_delete=models.PROTECT, related_name='reversal')` |
| `effective_date` (L621) | `models.DateField(db_index=True)` |
| `request_key` (L622) | `models.CharField(max_length=120)` |
| `request_fingerprint` (L623) | `models.CharField(max_length=64)` |
| `reason` (L624) | `models.TextField()` |
| `actor` (L625) | `models.ForeignKey(settings.AUTH_USER_MODEL, null=True, blank=True, on_delete=models.SET_NULL, related_name='funding_pledge_reversals_created')` |
| `created_at` (L632) | `models.DateTimeField(auto_now_add=True)` |

Named constraints:

- L636: `models.UniqueConstraint(fields=('funding_pledge', 'request_key'), name='loans_funding_pledge_reversal_request_uniq')`

Methods/properties: clean (L642).

## FundingReturnReversal

[apps/tenant_apps/loans/models/funding.py](../../apps/tenant_apps/loans/models/funding.py), line 648. Bases: `ImmutableEvidenceModel`.

| Field | Declaration (type, optionality, relation, deletion, default, precision) |
| --- | --- |
| `funding_return` (L649) | `models.OneToOneField(FundingReturn, on_delete=models.PROTECT, related_name='reversal')` |
| `effective_date` (L654) | `models.DateField(db_index=True)` |
| `request_key` (L655) | `models.CharField(max_length=120)` |
| `request_fingerprint` (L656) | `models.CharField(max_length=64)` |
| `reason` (L657) | `models.TextField()` |
| `actor` (L658) | `models.ForeignKey(settings.AUTH_USER_MODEL, null=True, blank=True, on_delete=models.SET_NULL, related_name='funding_return_reversals_created')` |
| `created_at` (L665) | `models.DateTimeField(auto_now_add=True)` |

Named constraints:

- L669: `models.UniqueConstraint(fields=('funding_return', 'request_key'), name='loans_funding_return_reversal_request_uniq')`

Methods/properties: clean (L675).

## HistoricalLoanImport

[apps/tenant_apps/loans/models/history.py](../../apps/tenant_apps/loans/models/history.py), line 8. Bases: `WorkspaceOwnedModel`.

| Field | Declaration (type, optionality, relation, deletion, default, precision) |
| --- | --- |
| `public_id` (L9) | `models.UUIDField(default=uuid.uuid4, unique=True, editable=False)` |
| `loan` (L10) | `models.OneToOneField('loans.PawnLoan', on_delete=models.PROTECT, related_name='historical_import')` |
| `source_namespace` (L11) | `models.UUIDField()` |
| `source_id` (L12) | `models.CharField(max_length=120)` |
| `source_sha256` (L13) | `models.CharField(max_length=64)` |
| `document` (L14) | `models.JSONField()` |
| `references` (L15) | `models.JSONField()` |
| `imported_by` (L16) | `models.ForeignKey(settings.AUTH_USER_MODEL, on_delete=models.PROTECT)` |
| `imported_at` (L17) | `models.DateTimeField(auto_now_add=True)` |

Named constraints:

- L20: `models.UniqueConstraint(fields=['workspace', 'source_namespace', 'source_id'], name='loans_history_source_unique')`

Methods/properties: none declared; inherited behavior still applies.

## PawnCollateralPhoto

[apps/tenant_apps/loans/models/media.py](../../apps/tenant_apps/loans/models/media.py), line 20. Bases: `WorkspaceOwnedModel`. ordering=('collateral_item_id', 'captured_at', 'id')

| Field | Declaration (type, optionality, relation, deletion, default, precision) |
| --- | --- |
| `collateral_item` (L27) | `models.ForeignKey('loans.PawnCollateralItem', on_delete=models.PROTECT, related_name='photos')` |
| `file` (L32) | `models.FileField(upload_to=collateral_photo_upload_to, max_length=500)` |
| `original_filename` (L33) | `models.CharField(max_length=255)` |
| `mime_type` (L34) | `models.CharField(max_length=100)` |
| `sha256` (L35) | `models.CharField(max_length=64)` |
| `byte_size` (L36) | `models.PositiveBigIntegerField()` |
| `workflow_source` (L37) | `models.CharField(max_length=24, choices=WorkflowSource.choices)` |
| `inherited_from` (L38) | `models.ForeignKey('self', null=True, blank=True, on_delete=models.PROTECT, related_name='renewal_copies')` |
| `captured_by` (L45) | `models.ForeignKey(settings.AUTH_USER_MODEL, null=True, blank=True, on_delete=models.SET_NULL, related_name='pawn_collateral_photos_captured')` |
| `captured_at` (L52) | `models.DateTimeField(auto_now_add=True)` |

Methods/properties: save (L63), delete (L70).

## PawnCollateralLabelIssue

[apps/tenant_apps/loans/models/media.py](../../apps/tenant_apps/loans/models/media.py), line 74. Bases: `WorkspaceOwnedModel`. ordering=('collateral_item_id', 'issued_at', 'id')

| Field | Declaration (type, optionality, relation, deletion, default, precision) |
| --- | --- |
| `collateral_item` (L79) | `models.ForeignKey('loans.PawnCollateralItem', on_delete=models.PROTECT, related_name='label_issues')` |
| `action` (L84) | `models.CharField(max_length=12, choices=Action.choices)` |
| `qr_target` (L85) | `models.CharField(max_length=500)` |
| `payload_sha256` (L86) | `models.CharField(max_length=64)` |
| `issued_by` (L87) | `models.ForeignKey(settings.AUTH_USER_MODEL, null=True, blank=True, on_delete=models.SET_NULL, related_name='pawn_collateral_labels_issued')` |
| `issued_at` (L94) | `models.DateTimeField(auto_now_add=True)` |

Methods/properties: save (L105), delete (L110).

## LoanMonitoringPolicy

[apps/tenant_apps/loans/models/monitoring.py](../../apps/tenant_apps/loans/models/monitoring.py), line 9. Bases: `models.Model`. ordering=('workspace_id', 'license_id', 'effective_from', 'version')

| Field | Declaration (type, optionality, relation, deletion, default, precision) |
| --- | --- |
| `workspace` (L10) | `models.ForeignKey('orgs.Company', on_delete=models.PROTECT, related_name='loan_monitoring_policies')` |
| `license` (L11) | `models.ForeignKey('loans.LoanLicense', null=True, blank=True, on_delete=models.PROTECT, related_name='monitoring_policies')` |
| `version` (L12) | `models.PositiveIntegerField()` |
| `effective_from` (L13) | `models.DateField()` |
| `effective_until` (L14) | `models.DateField(null=True, blank=True)` |
| `supersedes` (L15) | `models.OneToOneField('self', null=True, blank=True, on_delete=models.PROTECT, related_name='successor')` |
| `amendment_reason` (L16) | `models.TextField(blank=True)` |
| `compliance_profile` (L17) | `models.CharField(max_length=120)` |
| `maturity_warning_days` (L18) | `models.PositiveSmallIntegerField(default=30)` |
| `operational_grace_days` (L19) | `models.PositiveSmallIntegerField(default=3)` |
| `dpd_watch_threshold` (L20) | `models.PositiveSmallIntegerField(default=1)` |
| `dpd_substandard_threshold` (L21) | `models.PositiveSmallIntegerField(default=90)` |
| `ltv_warning_ratio` (L22) | `models.DecimalField(max_digits=7, decimal_places=6)` |
| `ltv_breach_ratio` (L23) | `models.DecimalField(max_digits=7, decimal_places=6)` |
| `ltv_critical_ratio` (L24) | `models.DecimalField(max_digits=7, decimal_places=6)` |
| `rate_freshness_days` (L25) | `models.PositiveSmallIntegerField(default=7)` |
| `appraisal_freshness_days` (L26) | `models.PositiveSmallIntegerField(default=90)` |
| `eligible_custody_states` (L27) | `models.JSONField(default=list)` |
| `severity_mapping` (L28) | `models.JSONField(default=dict)` |
| `created_at` (L29) | `models.DateTimeField(auto_now_add=True)` |
| `created_by` (L30) | `models.ForeignKey(settings.AUTH_USER_MODEL, null=True, blank=True, on_delete=models.PROTECT, related_name='loan_monitoring_policies_created')` |

Named constraints:

- L35: `models.UniqueConstraint(fields=('workspace', 'license', 'version'), name='loans_monitor_policy_scope_version_uniq', nulls_distinct=False)`
- L36: `models.CheckConstraint(condition=Q(effective_until__isnull=True) | Q(effective_until__gte=F('effective_from')), name='loans_monitor_policy_dates_valid')`
- L37: `models.CheckConstraint(condition=Q(ltv_warning_ratio__gt=0) & Q(ltv_warning_ratio__lte=1), name='loans_monitor_ltv_warning_range')`
- L38: `models.CheckConstraint(condition=Q(ltv_breach_ratio__gt=0) & Q(ltv_breach_ratio__lte=1), name='loans_monitor_ltv_breach_range')`
- L39: `models.CheckConstraint(condition=Q(ltv_critical_ratio__gt=0), name='loans_monitor_ltv_critical_positive')`

Methods/properties: clean (L43), save (L81), delete (L87).

## ImmutableObligationEvidence

[apps/tenant_apps/loans/models/obligations.py](../../apps/tenant_apps/loans/models/obligations.py), line 10. Bases: `models.Model`. abstract=True

| Field | Declaration (type, optionality, relation, deletion, default, precision) |
| --- | --- |

Methods/properties: save (L14), delete (L20).

## RepaymentScheduleVersion

[apps/tenant_apps/loans/models/obligations.py](../../apps/tenant_apps/loans/models/obligations.py), line 24. Bases: `ImmutableObligationEvidence`. ordering=('loan_id', 'version')

| Field | Declaration (type, optionality, relation, deletion, default, precision) |
| --- | --- |
| `workspace` (L25) | `models.ForeignKey('orgs.Company', on_delete=models.PROTECT, related_name='repayment_schedule_versions')` |
| `loan` (L28) | `models.ForeignKey('loans.PawnLoan', on_delete=models.PROTECT, related_name='repayment_schedules')` |
| `source_event` (L31) | `models.OneToOneField('loans.PawnLoanEvent', on_delete=models.PROTECT, related_name='repayment_schedule')` |
| `version` (L36) | `models.PositiveIntegerField(default=1)` |
| `contract_version` (L37) | `models.CharField(max_length=40)` |
| `fingerprint` (L38) | `models.CharField(max_length=64)` |
| `disbursed_on` (L39) | `models.DateField()` |
| `maturity_date` (L40) | `models.DateField()` |
| `principal` (L41) | `models.DecimalField(max_digits=18, decimal_places=4)` |
| `contractual_interest` (L42) | `models.DecimalField(max_digits=18, decimal_places=4)` |
| `rounding_adjustment` (L43) | `models.DecimalField(max_digits=18, decimal_places=4, default=0)` |
| `supersedes` (L44) | `models.ForeignKey('self', null=True, blank=True, on_delete=models.PROTECT, related_name='superseded_by')` |
| `created_at` (L47) | `models.DateTimeField(auto_now_add=True)` |
| `created_by` (L48) | `models.ForeignKey(settings.AUTH_USER_MODEL, null=True, blank=True, on_delete=models.SET_NULL, related_name='repayment_schedule_versions_created')` |

Named constraints:

- L59: `models.UniqueConstraint(fields=('loan', 'version'), name='loans_schedule_loan_version_uniq')`
- L60: `models.UniqueConstraint(fields=('loan', 'fingerprint'), name='loans_schedule_loan_fingerprint_uniq')`
- L61: `models.CheckConstraint(condition=Q(principal__gt=0), name='loans_schedule_principal_positive')`
- L62: `models.CheckConstraint(condition=Q(contractual_interest__gte=0), name='loans_schedule_interest_nonnegative')`
- L63: `models.CheckConstraint(condition=Q(maturity_date__gte=F('disbursed_on')), name='loans_schedule_dates_valid')`

Methods/properties: clean (L67).

## RepaymentObligation

[apps/tenant_apps/loans/models/obligations.py](../../apps/tenant_apps/loans/models/obligations.py), line 83. Bases: `ImmutableObligationEvidence`. ordering=('due_date', 'sequence')

| Field | Declaration (type, optionality, relation, deletion, default, precision) |
| --- | --- |
| `workspace` (L84) | `models.ForeignKey('orgs.Company', on_delete=models.PROTECT, related_name='repayment_obligations')` |
| `loan` (L87) | `models.ForeignKey('loans.PawnLoan', on_delete=models.PROTECT, related_name='repayment_obligations')` |
| `schedule_version` (L90) | `models.ForeignKey(RepaymentScheduleVersion, on_delete=models.PROTECT, related_name='obligations')` |
| `sequence` (L93) | `models.PositiveIntegerField()` |
| `due_date` (L94) | `models.DateField(db_index=True)` |
| `principal_due` (L95) | `models.DecimalField(max_digits=18, decimal_places=4, default=0)` |
| `interest_due` (L96) | `models.DecimalField(max_digits=18, decimal_places=4, default=0)` |
| `opening_principal` (L97) | `models.DecimalField(max_digits=18, decimal_places=4)` |
| `closing_principal` (L98) | `models.DecimalField(max_digits=18, decimal_places=4)` |
| `created_at` (L99) | `models.DateTimeField(auto_now_add=True)` |

Named constraints:

- L104: `models.UniqueConstraint(fields=('schedule_version', 'sequence'), name='loans_obligation_schedule_sequence_uniq')`
- L105: `models.CheckConstraint(condition=Q(principal_due__gte=0), name='loans_obligation_principal_nonnegative')`
- L106: `models.CheckConstraint(condition=Q(interest_due__gte=0), name='loans_obligation_interest_nonnegative')`
- L107: `models.CheckConstraint(condition=Q(opening_principal__gte=0), name='loans_obligation_opening_nonnegative')`
- L108: `models.CheckConstraint(condition=Q(closing_principal__gte=0), name='loans_obligation_closing_nonnegative')`
- L109: `models.CheckConstraint(condition=Q(principal_due__gt=0) | Q(interest_due__gt=0), name='loans_obligation_amount_positive')`

Methods/properties: clean (L113).

## RepaymentScheduleChange

[apps/tenant_apps/loans/models/obligations.py](../../apps/tenant_apps/loans/models/obligations.py), line 133. Bases: `ImmutableObligationEvidence`. ordering=('effective_date', 'pk')

| Field | Declaration (type, optionality, relation, deletion, default, precision) |
| --- | --- |
| `workspace` (L134) | `models.ForeignKey('orgs.Company', on_delete=models.PROTECT, related_name='repayment_schedule_changes')` |
| `loan` (L137) | `models.ForeignKey('loans.PawnLoan', on_delete=models.PROTECT, related_name='repayment_schedule_changes')` |
| `schedule_version` (L140) | `models.ForeignKey(RepaymentScheduleVersion, on_delete=models.PROTECT, related_name='changes')` |
| `source_event` (L143) | `models.OneToOneField('loans.PawnLoanEvent', on_delete=models.PROTECT, related_name='repayment_schedule_change')` |
| `kind` (L148) | `models.CharField(max_length=12, choices=RepaymentScheduleChangeKind.choices)` |
| `effective_date` (L149) | `models.DateField()` |
| `reason` (L150) | `models.CharField(max_length=120)` |
| `reversal_of` (L151) | `models.OneToOneField('self', null=True, blank=True, on_delete=models.PROTECT, related_name='reversal')` |
| `created_at` (L154) | `models.DateTimeField(auto_now_add=True)` |
| `created_by` (L155) | `models.ForeignKey(settings.AUTH_USER_MODEL, null=True, blank=True, on_delete=models.SET_NULL, related_name='repayment_schedule_changes_created')` |

Methods/properties: clean (L172).

## ObligationAllocation

[apps/tenant_apps/loans/models/obligations.py](../../apps/tenant_apps/loans/models/obligations.py), line 195. Bases: `ImmutableObligationEvidence`. ordering=('source_event_id', 'allocation_order')

| Field | Declaration (type, optionality, relation, deletion, default, precision) |
| --- | --- |
| `workspace` (L196) | `models.ForeignKey('orgs.Company', on_delete=models.PROTECT, related_name='obligation_allocations')` |
| `loan` (L199) | `models.ForeignKey('loans.PawnLoan', on_delete=models.PROTECT, related_name='obligation_allocations')` |
| `source_event` (L202) | `models.ForeignKey('loans.PawnLoanEvent', on_delete=models.PROTECT, related_name='obligation_allocations')` |
| `obligation` (L205) | `models.ForeignKey(RepaymentObligation, on_delete=models.PROTECT, related_name='allocations')` |
| `component` (L208) | `models.CharField(max_length=16, choices=enum_choices(ObligationComponent))` |
| `amount` (L209) | `models.DecimalField(max_digits=18, decimal_places=4)` |
| `allocation_order` (L210) | `models.PositiveIntegerField()` |
| `reversal_of` (L211) | `models.OneToOneField('self', null=True, blank=True, on_delete=models.PROTECT, related_name='reversal')` |
| `created_at` (L214) | `models.DateTimeField(auto_now_add=True)` |
| `created_by` (L215) | `models.ForeignKey(settings.AUTH_USER_MODEL, null=True, blank=True, on_delete=models.SET_NULL, related_name='obligation_allocations_created')` |

Named constraints:

- L226: `models.UniqueConstraint(fields=('source_event', 'allocation_order'), name='loans_allocation_event_order_uniq')`
- L227: `models.CheckConstraint(condition=~Q(amount=0), name='loans_allocation_amount_nonzero')`

Methods/properties: clean (L231).

## LoanOperationalNotice

[apps/tenant_apps/loans/models/operational_notices.py](../../apps/tenant_apps/loans/models/operational_notices.py), line 7. Bases: `models.Model`. ordering=('-created_at', '-id')

| Field | Declaration (type, optionality, relation, deletion, default, precision) |
| --- | --- |
| `workspace` (L15) | `models.ForeignKey('orgs.Company', on_delete=models.PROTECT, related_name='loan_operational_notices')` |
| `notice_kind` (L20) | `models.CharField(max_length=32, choices=Kind.choices)` |
| `request_key` (L21) | `models.CharField(max_length=120)` |
| `scheduled_for` (L22) | `models.DateTimeField(db_index=True)` |
| `recipient_name` (L23) | `models.CharField(max_length=255)` |
| `recipient_email` (L24) | `models.EmailField()` |
| `payload_snapshot` (L25) | `models.JSONField(default=dict)` |
| `notification_event_id` (L26) | `models.PositiveBigIntegerField(null=True, blank=True)` |
| `notification_job_id` (L27) | `models.PositiveBigIntegerField(null=True, blank=True)` |
| `source_license` (L28) | `models.ForeignKey('loans.LoanLicense', null=True, blank=True, on_delete=models.PROTECT, related_name='operational_notices')` |
| `source_verification_observation` (L35) | `models.ForeignKey('loans.PawnPhysicalVerificationObservation', null=True, blank=True, on_delete=models.PROTECT, related_name='operational_notices')` |
| `created_by` (L42) | `models.ForeignKey(settings.AUTH_USER_MODEL, null=True, blank=True, on_delete=models.SET_NULL, related_name='loan_operational_notices_created')` |
| `created_at` (L49) | `models.DateTimeField(auto_now_add=True)` |

Named constraints:

- L54: `models.UniqueConstraint(fields=('workspace', 'request_key'), name='loans_operational_notice_request_uniq')`
- L58: `models.CheckConstraint(condition=Q(notice_kind='LICENSE_EXPIRY', source_license__isnull=False, source_verification_observation__isnull=True) | Q(notice_kind='VERIFICATION_DISCREPANCY', source_license__isnull=True, source_verification_observation__isnull=False), name='loans_operational_notice_source_shape')`

Methods/properties: save (L81), delete (L87).

## LoanProduct

[apps/tenant_apps/loans/models/products.py](../../apps/tenant_apps/loans/models/products.py), line 18. Bases: `models.Model`. ordering=('workspace_id', 'code')

| Field | Declaration (type, optionality, relation, deletion, default, precision) |
| --- | --- |
| `workspace` (L19) | `models.ForeignKey('orgs.Company', on_delete=models.PROTECT, related_name='loan_products')` |
| `code` (L22) | `models.CharField(max_length=40)` |
| `name` (L23) | `models.CharField(max_length=120)` |
| `is_active` (L24) | `models.BooleanField(default=True, db_index=True)` |
| `created_at` (L25) | `models.DateTimeField(auto_now_add=True)` |
| `updated_at` (L26) | `models.DateTimeField(auto_now=True)` |
| `created_by` (L27) | `models.ForeignKey(settings.AUTH_USER_MODEL, null=True, blank=True, on_delete=models.SET_NULL, related_name='loan_products_created')` |
| `updated_by` (L34) | `models.ForeignKey(settings.AUTH_USER_MODEL, null=True, blank=True, on_delete=models.SET_NULL, related_name='loan_products_updated')` |

Named constraints:

- L45: `models.UniqueConstraint(fields=('workspace', 'code'), name='loans_product_workspace_code_uniq')`

Methods/properties: clean (L55), save (L61), __str__ (L65).

## LoanProductVersion

[apps/tenant_apps/loans/models/products.py](../../apps/tenant_apps/loans/models/products.py), line 69. Bases: `WorkspaceOwnedModel`. ordering=('product_id', 'version')

| Field | Declaration (type, optionality, relation, deletion, default, precision) |
| --- | --- |
| `product` (L70) | `models.ForeignKey(LoanProduct, on_delete=models.PROTECT, related_name='versions')` |
| `version` (L73) | `models.PositiveIntegerField()` |
| `status` (L74) | `models.CharField(max_length=12, choices=enum_choices(LoanProductVersionStatus), default=LoanProductVersionStatus.DRAFT.value, db_index=True)` |
| `available_from` (L80) | `models.DateField(null=True, blank=True)` |
| `available_until` (L81) | `models.DateField(null=True, blank=True)` |
| `repayment_structure` (L82) | `models.CharField(max_length=32, choices=enum_choices(LoanRepaymentStructure))` |
| `amortisation_method` (L85) | `models.CharField(max_length=24, choices=enum_choices(LoanAmortisationMethod), default=LoanAmortisationMethod.NONE.value)` |
| `payment_frequency` (L90) | `models.CharField(max_length=16, choices=enum_choices(LoanPaymentFrequency))` |
| `minimum_tenor_months` (L93) | `models.PositiveIntegerField(default=1)` |
| `maximum_tenor_months` (L94) | `models.PositiveIntegerField()` |
| `operational_grace_days` (L95) | `models.PositiveSmallIntegerField(default=3)` |
| `extra_payment_rule` (L96) | `models.CharField(max_length=32, choices=enum_choices(LoanExtraPaymentRule))` |
| `calculation_contract_version` (L99) | `models.CharField(max_length=40)` |
| `created_at` (L100) | `models.DateTimeField(auto_now_add=True)` |
| `created_by` (L101) | `models.ForeignKey(settings.AUTH_USER_MODEL, null=True, blank=True, on_delete=models.SET_NULL, related_name='loan_product_versions_created')` |

Named constraints:

- L112: `models.UniqueConstraint(fields=('product', 'version'), name='loans_product_version_uniq')`
- L115: `models.UniqueConstraint(fields=('product',), condition=Q(status=LoanProductVersionStatus.ACTIVE.value), name='loans_product_one_active_uniq')`
- L120: `models.CheckConstraint(condition=Q(maximum_tenor_months__gte=F('minimum_tenor_months')), name='loans_product_tenor_range_valid')`
- L124: `models.CheckConstraint(condition=Q(operational_grace_days__lte=30), name='loans_product_grace_reasonable')`

Methods/properties: clean (L136), save (L153), delete (L174), __str__ (L177).

## LoanLicenseRevision

[apps/tenant_apps/loans/models/regulatory.py](../../apps/tenant_apps/loans/models/regulatory.py), line 24. Bases: `WorkspaceOwnedModel`. ordering=('license_id', 'revision_number')

| Field | Declaration (type, optionality, relation, deletion, default, precision) |
| --- | --- |
| `license` (L31) | `models.ForeignKey(LoanLicense, on_delete=models.PROTECT, related_name='revisions')` |
| `revision_number` (L36) | `models.PositiveIntegerField()` |
| `kind` (L37) | `models.CharField(max_length=16, choices=Kind.choices)` |
| `name` (L38) | `models.CharField(max_length=255)` |
| `license_number` (L39) | `models.CharField(max_length=100)` |
| `issuing_authority` (L40) | `models.CharField(max_length=255, blank=True)` |
| `issued_on` (L41) | `models.DateField(null=True, blank=True)` |
| `expires_on` (L42) | `models.DateField(null=True, blank=True)` |
| `notes` (L43) | `models.TextField(blank=True)` |
| `supporting_document` (L44) | `models.FileField(upload_to=loan_license_document_upload, blank=True)` |
| `original_filename` (L48) | `models.CharField(max_length=255, blank=True)` |
| `mime_type` (L49) | `models.CharField(max_length=100, blank=True)` |
| `sha256` (L50) | `models.CharField(max_length=64, blank=True)` |
| `byte_size` (L51) | `models.PositiveIntegerField(default=0)` |
| `created_at` (L52) | `models.DateTimeField(auto_now_add=True)` |
| `created_by` (L53) | `models.ForeignKey(settings.AUTH_USER_MODEL, null=True, blank=True, on_delete=models.SET_NULL, related_name='loan_license_revisions_created')` |

Named constraints:

- L64: `models.UniqueConstraint(fields=('license', 'revision_number'), name='loans_license_revision_number_uniq')`
- L68: `models.CheckConstraint(condition=Q(revision_number__gt=0), name='loans_license_revision_number_positive')`
- L72: `models.CheckConstraint(condition=Q(expires_on__gte=F('issued_on')), name='loans_license_revision_dates_valid')`
- L76: `models.CheckConstraint(condition=Q(kind='LEGACY_REFERENCE', issued_on__isnull=True, expires_on__isnull=True) | ~Q(kind='LEGACY_REFERENCE') & Q(issued_on__isnull=False, expires_on__isnull=False), name='loans_licrev_validity_evidence')`
- L81: `models.CheckConstraint(condition=Q(supporting_document='', sha256='', byte_size=0) | ~Q(supporting_document='') & ~Q(sha256='') & Q(byte_size__gt=0), name='loans_license_revision_document_complete')`

Methods/properties: has_document (L105), is_legacy_reference (L109), clean (L112), save (L125), delete (L129), __str__ (L132).

## PawnReleaseBatch

[apps/tenant_apps/loans/models/release_batch.py](../../apps/tenant_apps/loans/models/release_batch.py), line 7. Bases: `WorkspaceOwnedModel`. ordering=('-id',)

| Field | Declaration (type, optionality, relation, deletion, default, precision) |
| --- | --- |
| `request_key` (L8) | `models.UUIDField()` |
| `request_fingerprint` (L9) | `models.CharField(max_length=64)` |
| `effective_date` (L10) | `models.DateField()` |
| `total_amount` (L11) | `models.DecimalField(max_digits=18, decimal_places=4)` |
| `paid_by` (L12) | `models.CharField(max_length=255)` |
| `payment_reference` (L13) | `models.CharField(max_length=100, blank=True)` |
| `created_by` (L14) | `models.ForeignKey(settings.AUTH_USER_MODEL, on_delete=models.PROTECT)` |
| `created_at` (L15) | `models.DateTimeField(auto_now_add=True)` |

Named constraints:

- L20: `models.UniqueConstraint(fields=('workspace', 'request_key'), name='loans_batch_workspace_request_uniq')`
- L21: `models.CheckConstraint(condition=models.Q(total_amount__gte=0), name='loans_batch_total_nonnegative')`

Methods/properties: none declared; inherited behavior still applies.

## PawnReleaseBatchLine

[apps/tenant_apps/loans/models/release_batch.py](../../apps/tenant_apps/loans/models/release_batch.py), line 25. Bases: `WorkspaceOwnedModel`. ordering=('id',)

| Field | Declaration (type, optionality, relation, deletion, default, precision) |
| --- | --- |
| `batch` (L26) | `models.ForeignKey(PawnReleaseBatch, on_delete=models.PROTECT, related_name='lines')` |
| `release` (L27) | `models.OneToOneField('loans.PawnLoanRelease', on_delete=models.PROTECT, related_name='batch_line')` |
| `borrower_name` (L28) | `models.CharField(max_length=255)` |
| `collector_name` (L29) | `models.CharField(max_length=255)` |
| `collector_is_borrower` (L30) | `models.BooleanField()` |
| `relationship` (L31) | `models.CharField(max_length=100, blank=True)` |
| `authorization_note` (L32) | `models.CharField(max_length=500, blank=True)` |

Methods/properties: none declared; inherited behavior still applies.

## LoanRiskSnapshot

[apps/tenant_apps/loans/models/risk.py](../../apps/tenant_apps/loans/models/risk.py), line 6. Bases: `models.Model`.

| Field | Declaration (type, optionality, relation, deletion, default, precision) |
| --- | --- |
| `workspace` (L12) | `models.ForeignKey('orgs.Company', on_delete=models.CASCADE, related_name='loan_risk_snapshots')` |
| `loan` (L13) | `models.OneToOneField('loans.PawnLoan', on_delete=models.CASCADE, related_name='risk_snapshot')` |
| `as_of_date` (L14) | `models.DateField()` |
| `exposure` (L15) | `models.DecimalField(max_digits=18, decimal_places=4, null=True)` |
| `due` (L16) | `models.DecimalField(max_digits=18, decimal_places=4, null=True)` |
| `overdue` (L17) | `models.DecimalField(max_digits=18, decimal_places=4, null=True)` |
| `days_past_due` (L18) | `models.PositiveIntegerField(null=True)` |
| `days_to_maturity` (L19) | `models.IntegerField(null=True)` |
| `collateral_value` (L20) | `models.DecimalField(max_digits=18, decimal_places=4, null=True)` |
| `ltv_ratio` (L21) | `models.DecimalField(max_digits=12, decimal_places=8, null=True)` |
| `performance_class` (L22) | `models.CharField(max_length=24, blank=True)` |
| `severity` (L23) | `models.CharField(max_length=16, blank=True)` |
| `action_hint` (L24) | `models.CharField(max_length=32, blank=True)` |
| `flags` (L25) | `models.JSONField(default=list)` |
| `explanations` (L26) | `models.JSONField(default=list)` |
| `policy_identity` (L27) | `models.CharField(max_length=255, blank=True)` |
| `source_provenance` (L28) | `models.JSONField(default=dict)` |
| `assessment_fingerprint` (L29) | `models.CharField(max_length=64, blank=True)` |
| `input_fingerprint` (L30) | `models.CharField(max_length=64, blank=True)` |
| `status` (L31) | `models.CharField(max_length=12, choices=Status.choices, default=Status.STALE, db_index=True)` |
| `error_message` (L32) | `models.TextField(blank=True)` |
| `assessed_at` (L33) | `models.DateTimeField(null=True)` |
| `updated_at` (L34) | `models.DateTimeField(auto_now=True)` |

Named constraints:

- L38: `models.UniqueConstraint(fields=('workspace', 'loan'), name='loans_risk_snapshot_workspace_loan_uniq')`
- L39: `models.CheckConstraint(condition=Q(status='ERROR') | ~Q(assessment_fingerprint=''), name='loans_risk_current_has_assessment')`

Methods/properties: none declared; inherited behavior still applies.

## LoanRiskEvent

[apps/tenant_apps/loans/models/risk.py](../../apps/tenant_apps/loans/models/risk.py), line 47. Bases: `models.Model`. ordering=('occurred_at', 'pk')

| Field | Declaration (type, optionality, relation, deletion, default, precision) |
| --- | --- |
| `workspace` (L48) | `models.ForeignKey('orgs.Company', on_delete=models.PROTECT, related_name='loan_risk_events')` |
| `loan` (L49) | `models.ForeignKey('loans.PawnLoan', on_delete=models.PROTECT, related_name='risk_events')` |
| `snapshot` (L50) | `models.ForeignKey(LoanRiskSnapshot, on_delete=models.PROTECT, related_name='transition_events')` |
| `event_type` (L51) | `models.CharField(max_length=40)` |
| `occurred_at` (L52) | `models.DateTimeField(auto_now_add=True)` |
| `as_of_date` (L53) | `models.DateField()` |
| `old_value` (L54) | `models.JSONField(default=dict)` |
| `new_value` (L55) | `models.JSONField(default=dict)` |
| `old_fingerprint` (L56) | `models.CharField(max_length=64, blank=True)` |
| `new_fingerprint` (L57) | `models.CharField(max_length=64, blank=True)` |
| `policy_identity` (L58) | `models.CharField(max_length=255, blank=True)` |
| `trigger_key` (L59) | `models.CharField(max_length=64)` |
| `metadata` (L60) | `models.JSONField(default=dict)` |

Named constraints:

- L64: `models.UniqueConstraint(fields=('workspace', 'loan', 'trigger_key'), name='loans_risk_event_trigger_uniq')`

Methods/properties: save (L67), delete (L72).

## LoanRiskAlert

[apps/tenant_apps/loans/models/risk.py](../../apps/tenant_apps/loans/models/risk.py), line 76. Bases: `models.Model`. ordering=('-created_at', '-pk')

| Field | Declaration (type, optionality, relation, deletion, default, precision) |
| --- | --- |
| `workspace` (L87) | `models.ForeignKey('orgs.Company', on_delete=models.PROTECT, related_name='loan_risk_alerts')` |
| `loan` (L90) | `models.ForeignKey('loans.PawnLoan', on_delete=models.PROTECT, related_name='risk_alerts')` |
| `source_event` (L93) | `models.ForeignKey(LoanRiskEvent, on_delete=models.PROTECT, related_name='operational_alerts')` |
| `resolved_by_event` (L96) | `models.ForeignKey(LoanRiskEvent, null=True, blank=True, on_delete=models.PROTECT, related_name='resolved_operational_alerts')` |
| `alert_kind` (L100) | `models.CharField(max_length=24, choices=Kind.choices)` |
| `severity` (L101) | `models.CharField(max_length=16)` |
| `message` (L102) | `models.CharField(max_length=500)` |
| `recommended_action` (L103) | `models.CharField(max_length=255)` |
| `status` (L104) | `models.CharField(max_length=12, choices=Status.choices, default=Status.OPEN, db_index=True)` |
| `created_at` (L107) | `models.DateTimeField(auto_now_add=True)` |
| `resolved_at` (L108) | `models.DateTimeField(null=True, blank=True)` |

Named constraints:

- L113: `models.UniqueConstraint(fields=('source_event', 'alert_kind'), name='loans_risk_alert_event_kind_uniq')`
- L117: `models.UniqueConstraint(fields=('workspace', 'loan', 'alert_kind'), condition=Q(status='OPEN'), name='loans_risk_alert_one_open_kind')`

Methods/properties: none declared; inherited behavior still applies.

## PawnStorageLocation

[apps/tenant_apps/loans/models/storage.py](../../apps/tenant_apps/loans/models/storage.py), line 13. Bases: `models.Model`. ordering=('level', 'code', 'id')

| Field | Declaration (type, optionality, relation, deletion, default, precision) |
| --- | --- |
| `public_id` (L21) | `models.UUIDField(default=uuid.uuid4, unique=True, editable=False)` |
| `workspace` (L22) | `models.ForeignKey('orgs.Company', on_delete=models.PROTECT, related_name='pawn_storage_locations')` |
| `parent` (L27) | `models.ForeignKey('self', null=True, blank=True, on_delete=models.PROTECT, related_name='children')` |
| `level` (L34) | `models.CharField(max_length=12, choices=Level.choices)` |
| `code` (L35) | `models.CharField(max_length=32)` |
| `name` (L36) | `models.CharField(max_length=100)` |
| `capacity` (L37) | `models.PositiveIntegerField(null=True, blank=True)` |
| `is_active` (L38) | `models.BooleanField(default=True)` |
| `created_by` (L39) | `models.ForeignKey(settings.AUTH_USER_MODEL, null=True, blank=True, on_delete=models.SET_NULL, related_name='pawn_storage_locations_created')` |
| `created_at` (L46) | `models.DateTimeField(auto_now_add=True)` |

Named constraints:

- L51: `models.UniqueConstraint(fields=('workspace', 'code'), name='loans_storage_workspace_code_uniq')`
- L55: `models.CheckConstraint(condition=Q(capacity__isnull=True) | Q(capacity__gt=0), name='loans_storage_capacity_positive')`

Methods/properties: clean (L67), save (L89), path_label (L94), __str__ (L102).

## PawnCollateralStorageMovement

[apps/tenant_apps/loans/models/storage.py](../../apps/tenant_apps/loans/models/storage.py), line 106. Bases: `WorkspaceOwnedModel`. ordering=('collateral_item_id', 'moved_at', 'id')

| Field | Declaration (type, optionality, relation, deletion, default, precision) |
| --- | --- |
| `collateral_item` (L112) | `models.ForeignKey('loans.PawnCollateralItem', on_delete=models.PROTECT, related_name='storage_movements')` |
| `from_location` (L117) | `models.ForeignKey(PawnStorageLocation, null=True, blank=True, on_delete=models.PROTECT, related_name='outbound_movements')` |
| `to_location` (L124) | `models.ForeignKey(PawnStorageLocation, null=True, blank=True, on_delete=models.PROTECT, related_name='inbound_movements')` |
| `kind` (L131) | `models.CharField(max_length=12, choices=Kind.choices)` |
| `reason` (L132) | `models.CharField(max_length=500, blank=True)` |
| `workflow_source` (L133) | `models.CharField(max_length=64, default='OPERATOR')` |
| `source_reference` (L134) | `models.CharField(max_length=120, blank=True)` |
| `moved_by` (L135) | `models.ForeignKey(settings.AUTH_USER_MODEL, null=True, blank=True, on_delete=models.SET_NULL, related_name='pawn_storage_movements_recorded')` |
| `moved_at` (L142) | `models.DateTimeField(auto_now_add=True)` |

Named constraints:

- L147: `models.CheckConstraint(condition=~(Q(from_location__isnull=True) & Q(to_location__isnull=True)), name='loans_storage_movement_has_endpoint')`
- L153: `models.CheckConstraint(condition=Q(from_location__isnull=True) | Q(to_location__isnull=True) | ~Q(from_location=models.F('to_location')), name='loans_storage_movement_changes')`

Methods/properties: save (L169), delete (L174).

## PawnPhysicalVerificationSession

[apps/tenant_apps/loans/models/verification.py](../../apps/tenant_apps/loans/models/verification.py), line 11. Bases: `models.Model`. ordering=('-started_at', '-id')

| Field | Declaration (type, optionality, relation, deletion, default, precision) |
| --- | --- |
| `public_id` (L16) | `models.UUIDField(default=uuid.uuid4, unique=True, editable=False)` |
| `workspace` (L17) | `models.ForeignKey('orgs.Company', on_delete=models.PROTECT, related_name='pawn_verification_sessions')` |
| `scope_location` (L20) | `models.ForeignKey('loans.PawnStorageLocation', on_delete=models.PROTECT, related_name='verification_sessions')` |
| `status` (L25) | `models.CharField(max_length=12, choices=Status.choices, default=Status.OPEN)` |
| `started_by` (L26) | `models.ForeignKey(settings.AUTH_USER_MODEL, null=True, on_delete=models.SET_NULL, related_name='pawn_verification_sessions_started')` |
| `started_at` (L32) | `models.DateTimeField(auto_now_add=True)` |
| `completed_by` (L33) | `models.ForeignKey(settings.AUTH_USER_MODEL, null=True, blank=True, on_delete=models.SET_NULL, related_name='pawn_verification_sessions_completed')` |
| `completed_at` (L40) | `models.DateTimeField(null=True, blank=True)` |

Methods/properties: save (L45), delete (L61).

## PawnPhysicalVerificationExpectation

[apps/tenant_apps/loans/models/verification.py](../../apps/tenant_apps/loans/models/verification.py), line 65. Bases: `WorkspaceOwnedModel`. ordering=('expected_location_id', 'collateral_item_id')

| Field | Declaration (type, optionality, relation, deletion, default, precision) |
| --- | --- |
| `session` (L66) | `models.ForeignKey(PawnPhysicalVerificationSession, on_delete=models.PROTECT, related_name='expectations')` |
| `collateral_item` (L71) | `models.ForeignKey('loans.PawnCollateralItem', on_delete=models.PROTECT, related_name='verification_expectations')` |
| `expected_location` (L76) | `models.ForeignKey('loans.PawnStorageLocation', on_delete=models.PROTECT, related_name='verification_expectations')` |
| `custody_state_snapshot` (L81) | `models.CharField(max_length=32)` |
| `loan_number_snapshot` (L82) | `models.CharField(max_length=96)` |
| `item_description_snapshot` (L83) | `models.CharField(max_length=255)` |
| `created_at` (L84) | `models.DateTimeField(auto_now_add=True)` |

Named constraints:

- L89: `models.UniqueConstraint(fields=('session', 'collateral_item'), name='loans_verification_expected_item_uniq')`

Methods/properties: save (L95), delete (L100).

## PawnPhysicalVerificationObservation

[apps/tenant_apps/loans/models/verification.py](../../apps/tenant_apps/loans/models/verification.py), line 104. Bases: `WorkspaceOwnedModel`. ordering=('recorded_at', 'id')

| Field | Declaration (type, optionality, relation, deletion, default, precision) |
| --- | --- |
| `session` (L111) | `models.ForeignKey(PawnPhysicalVerificationSession, on_delete=models.PROTECT, related_name='observations')` |
| `expectation` (L116) | `models.OneToOneField(PawnPhysicalVerificationExpectation, null=True, blank=True, on_delete=models.PROTECT, related_name='observation')` |
| `collateral_item` (L123) | `models.ForeignKey('loans.PawnCollateralItem', on_delete=models.PROTECT, related_name='verification_observations')` |
| `classification` (L128) | `models.CharField(max_length=12, choices=Classification.choices)` |
| `observed_location` (L129) | `models.ForeignKey('loans.PawnStorageLocation', null=True, blank=True, on_delete=models.PROTECT, related_name='verification_observations')` |
| `notes` (L136) | `models.CharField(max_length=500, blank=True)` |
| `recorded_by` (L137) | `models.ForeignKey(settings.AUTH_USER_MODEL, null=True, on_delete=models.SET_NULL, related_name='pawn_verification_observations_recorded')` |
| `recorded_at` (L143) | `models.DateTimeField(auto_now_add=True)` |

Named constraints:

- L148: `models.UniqueConstraint(fields=('session', 'collateral_item'), name='loans_verification_observed_item_uniq')`
- L152: `models.CheckConstraint(condition=Q(classification='UNEXPECTED', expectation__isnull=True) | ~Q(classification='UNEXPECTED') & Q(expectation__isnull=False), name='loans_verification_unexpected_shape')`

Methods/properties: save (L161), delete (L166).

## PawnPhysicalVerificationResolution

[apps/tenant_apps/loans/models/verification.py](../../apps/tenant_apps/loans/models/verification.py), line 170. Bases: `WorkspaceOwnedModel`. ordering=('resolved_at', 'id')

| Field | Declaration (type, optionality, relation, deletion, default, precision) |
| --- | --- |
| `observation` (L177) | `models.OneToOneField(PawnPhysicalVerificationObservation, on_delete=models.PROTECT, related_name='resolution')` |
| `outcome` (L182) | `models.CharField(max_length=24, choices=Outcome.choices)` |
| `reason` (L183) | `models.CharField(max_length=500)` |
| `current_market_value` (L184) | `models.DecimalField(max_digits=18, decimal_places=2, null=True, blank=True)` |
| `agreed_compensation` (L187) | `models.DecimalField(max_digits=18, decimal_places=2, null=True, blank=True)` |
| `compensation_reference` (L190) | `models.CharField(max_length=120, blank=True)` |
| `resolved_by` (L191) | `models.ForeignKey(settings.AUTH_USER_MODEL, null=True, on_delete=models.SET_NULL, related_name='pawn_verification_resolutions_recorded')` |
| `resolved_at` (L197) | `models.DateTimeField(auto_now_add=True)` |

Methods/properties: save (L202), delete (L207).

## WorkspaceNamespace

[apps/tenant_apps/data_portability/models.py](../../apps/tenant_apps/data_portability/models.py), line 11. Bases: `WorkspaceOwnedModel`.

| Field | Declaration (type, optionality, relation, deletion, default, precision) |
| --- | --- |
| `public_id` (L12) | `models.UUIDField(default=uuid.uuid4, unique=True, editable=False)` |

Named constraints:

- L15: `models.UniqueConstraint(fields=['workspace'], name='port_namespace_workspace_uniq')`

Methods/properties: none declared; inherited behavior still applies.

## ImportBatch

[apps/tenant_apps/data_portability/models.py](../../apps/tenant_apps/data_portability/models.py), line 18. Bases: `WorkspaceOwnedModel`.

| Field | Declaration (type, optionality, relation, deletion, default, precision) |
| --- | --- |
| `public_id` (L25) | `models.UUIDField(default=uuid.uuid4, unique=True, editable=False)` |
| `source_name` (L26) | `models.CharField(max_length=255)` |
| `source_type` (L27) | `models.CharField(max_length=8)` |
| `source_system` (L28) | `models.CharField(max_length=120)` |
| `source_sha256` (L29) | `models.CharField(max_length=64)` |
| `source_bytes` (L30) | `models.PositiveIntegerField()` |
| `contract_version` (L31) | `models.CharField(max_length=32, default='party-master/1')` |
| `headers` (L32) | `models.JSONField(default=list)` |
| `mapping` (L33) | `models.JSONField(default=dict)` |
| `mapping_preset` (L34) | `models.ForeignKey('MappingPresetVersion', null=True, on_delete=models.PROTECT, related_name='batches')` |
| `summary` (L35) | `models.JSONField(default=dict)` |
| `approval_digest` (L36) | `models.CharField(max_length=64, blank=True)` |
| `revision` (L37) | `models.PositiveIntegerField(default=0)` |
| `state` (L38) | `models.CharField(max_length=20, choices=State.choices, default=State.NEEDS_MAPPING)` |
| `created_by` (L39) | `models.ForeignKey(settings.AUTH_USER_MODEL, on_delete=models.PROTECT, related_name='+')` |
| `committed_by` (L40) | `models.ForeignKey(settings.AUTH_USER_MODEL, null=True, on_delete=models.PROTECT, related_name='+')` |
| `created_at` (L41) | `models.DateTimeField(auto_now_add=True)` |
| `committed_at` (L42) | `models.DateTimeField(null=True)` |

Methods/properties: none declared; inherited behavior still applies.

## PartyIdentity

[apps/tenant_apps/data_portability/models.py](../../apps/tenant_apps/data_portability/models.py), line 45. Bases: `WorkspaceOwnedModel`.

| Field | Declaration (type, optionality, relation, deletion, default, precision) |
| --- | --- |
| `public_id` (L46) | `models.UUIDField(default=uuid.uuid4, unique=True, editable=False)` |
| `party` (L47) | `models.OneToOneField('party.Party', on_delete=models.PROTECT, related_name='exchange_identity')` |
| `created_at` (L48) | `models.DateTimeField(auto_now_add=True)` |

Methods/properties: none declared; inherited behavior still applies.

## SourceIdentity

[apps/tenant_apps/data_portability/models.py](../../apps/tenant_apps/data_portability/models.py), line 51. Bases: `WorkspaceOwnedModel`.

| Field | Declaration (type, optionality, relation, deletion, default, precision) |
| --- | --- |
| `source_system` (L52) | `models.CharField(max_length=120)` |
| `external_id` (L53) | `models.CharField(max_length=255)` |
| `identity` (L54) | `models.ForeignKey(PartyIdentity, on_delete=models.PROTECT, related_name='sources')` |
| `accepted_digest` (L55) | `models.CharField(max_length=64)` |
| `local_digest` (L56) | `models.CharField(max_length=64)` |
| `created_at` (L57) | `models.DateTimeField(auto_now_add=True)` |

Named constraints:

- L60: `models.UniqueConstraint(fields=['workspace', 'source_system', 'external_id'], name='port_source_identity_uniq')`

Methods/properties: none declared; inherited behavior still applies.

## ImportRow

[apps/tenant_apps/data_portability/models.py](../../apps/tenant_apps/data_portability/models.py), line 63. Bases: `WorkspaceOwnedModel`.

| Field | Declaration (type, optionality, relation, deletion, default, precision) |
| --- | --- |
| `batch` (L64) | `models.ForeignKey(ImportBatch, on_delete=models.PROTECT, related_name='rows')` |
| `source_row` (L65) | `models.PositiveIntegerField()` |
| `raw` (L66) | `models.JSONField(default=dict)` |
| `canonical` (L67) | `models.JSONField(default=dict)` |
| `issues` (L68) | `models.JSONField(default=list)` |
| `external_id` (L69) | `models.CharField(max_length=255, blank=True)` |
| `disposition` (L70) | `models.CharField(max_length=16, default='UNVALIDATED')` |
| `identity` (L72) | `models.ForeignKey(PartyIdentity, null=True, on_delete=models.PROTECT, related_name='import_rows')` |
| `child_identity` (L73) | `models.ForeignKey('ChildIdentity', null=True, on_delete=models.PROTECT, related_name='import_rows')` |
| `committed_at` (L74) | `models.DateTimeField(null=True)` |

Named constraints:

- L77: `models.UniqueConstraint(fields=['workspace', 'batch', 'source_row'], name='port_batch_row_uniq')`

Methods/properties: none declared; inherited behavior still applies.

## ChildIdentity

[apps/tenant_apps/data_portability/models.py](../../apps/tenant_apps/data_portability/models.py), line 80. Bases: `WorkspaceOwnedModel`.

| Field | Declaration (type, optionality, relation, deletion, default, precision) |
| --- | --- |
| `public_id` (L81) | `models.UUIDField(default=uuid.uuid4, unique=True, editable=False)` |
| `parent` (L82) | `models.ForeignKey(PartyIdentity, on_delete=models.PROTECT, related_name='children')` |
| `profile` (L83) | `models.CharField(max_length=32)` |
| `related_parent` (L84) | `models.ForeignKey(PartyIdentity, null=True, on_delete=models.PROTECT, related_name='incoming_children')` |
| `relationship` (L85) | `models.OneToOneField('party.PartyRelationship', null=True, on_delete=models.SET_NULL, related_name='exchange_identity')` |
| `role` (L86) | `models.OneToOneField('party.PartyRole', null=True, on_delete=models.SET_NULL, related_name='exchange_identity')` |
| `identifier` (L87) | `models.OneToOneField('party.PartyIdentifier', null=True, on_delete=models.SET_NULL, related_name='exchange_identity')` |
| `contact` (L88) | `models.OneToOneField('party.PartyContactMethod', null=True, on_delete=models.SET_NULL, related_name='exchange_identity')` |
| `address` (L89) | `models.OneToOneField('party.PartyAddress', null=True, on_delete=models.SET_NULL, related_name='exchange_identity')` |
| `created_at` (L90) | `models.DateTimeField(auto_now_add=True)` |

Methods/properties: none declared; inherited behavior still applies.

## ChildSourceIdentity

[apps/tenant_apps/data_portability/models.py](../../apps/tenant_apps/data_portability/models.py), line 93. Bases: `WorkspaceOwnedModel`.

| Field | Declaration (type, optionality, relation, deletion, default, precision) |
| --- | --- |
| `profile` (L94) | `models.CharField(max_length=32)` |
| `source_system` (L95) | `models.CharField(max_length=120)` |
| `external_id` (L96) | `models.CharField(max_length=255)` |
| `identity` (L97) | `models.ForeignKey(ChildIdentity, on_delete=models.PROTECT, related_name='sources')` |
| `accepted_digest` (L98) | `models.CharField(max_length=64)` |
| `local_digest` (L99) | `models.CharField(max_length=64)` |
| `created_at` (L100) | `models.DateTimeField(auto_now_add=True)` |

Named constraints:

- L103: `models.UniqueConstraint(fields=['workspace', 'profile', 'source_system', 'external_id'], name='port_child_source_uniq')`

Methods/properties: none declared; inherited behavior still applies.

## MappingPresetVersion

[apps/tenant_apps/data_portability/models.py](../../apps/tenant_apps/data_portability/models.py), line 106. Bases: `WorkspaceOwnedModel`.

| Field | Declaration (type, optionality, relation, deletion, default, precision) |
| --- | --- |
| `public_id` (L107) | `models.UUIDField(default=uuid.uuid4, unique=True, editable=False)` |
| `name` (L108) | `models.CharField(max_length=80)` |
| `version` (L109) | `models.PositiveIntegerField()` |
| `profile` (L110) | `models.CharField(max_length=32)` |
| `source_system` (L111) | `models.CharField(max_length=120)` |
| `headers` (L112) | `models.JSONField(default=list)` |
| `mapping` (L113) | `models.JSONField(default=dict)` |
| `created_by` (L114) | `models.ForeignKey(settings.AUTH_USER_MODEL, on_delete=models.PROTECT, related_name='+')` |
| `created_at` (L115) | `models.DateTimeField(auto_now_add=True)` |

Named constraints:

- L119: `models.UniqueConstraint(fields=['workspace', 'name', 'version'], name='port_preset_version_uniq')`
- L120: `models.CheckConstraint(condition=models.Q(version__gte=1), name='port_preset_positive_version')`

Methods/properties: none declared; inherited behavior still applies.

## ImportBundle

[apps/tenant_apps/data_portability/models.py](../../apps/tenant_apps/data_portability/models.py), line 124. Bases: `WorkspaceOwnedModel`.

| Field | Declaration (type, optionality, relation, deletion, default, precision) |
| --- | --- |
| `public_id` (L126) | `models.UUIDField(default=uuid.uuid4, unique=True, editable=False)` |
| `source_namespace` (L127) | `models.UUIDField()` |
| `source_sha256` (L128) | `models.CharField(max_length=64)` |
| `created_by` (L129) | `models.ForeignKey(settings.AUTH_USER_MODEL, on_delete=models.PROTECT, related_name='+')` |
| `created_at` (L130) | `models.DateTimeField(default=timezone.now, editable=False)` |
| `master` (L131) | `models.OneToOneField(ImportBatch, null=True, on_delete=models.PROTECT, related_name='master_bundle')` |
| `contact` (L132) | `models.OneToOneField(ImportBatch, null=True, on_delete=models.PROTECT, related_name='contact_bundle')` |
| `address` (L133) | `models.OneToOneField(ImportBatch, null=True, on_delete=models.PROTECT, related_name='address_bundle')` |
| `identifier` (L134) | `models.OneToOneField(ImportBatch, null=True, on_delete=models.PROTECT, related_name='identifier_bundle')` |
| `role` (L135) | `models.OneToOneField(ImportBatch, null=True, on_delete=models.PROTECT, related_name='role_bundle')` |
| `relationship` (L136) | `models.OneToOneField(ImportBatch, null=True, on_delete=models.PROTECT, related_name='relationship_bundle')` |

Methods/properties: profile_batches (L140), receipt_entries (L143), progress (L147).

## LoanHistoryBatch

[apps/tenant_apps/data_portability/models.py](../../apps/tenant_apps/data_portability/models.py), line 160. Bases: `WorkspaceOwnedModel`.

| Field | Declaration (type, optionality, relation, deletion, default, precision) |
| --- | --- |
| `profile` (L161) | `models.CharField(max_length=32, default='loan-history/1', choices=[('loan-history/1', 'Complete history'), ('legacy-opening/1', 'Legacy opening')])` |
| `public_id` (L163) | `models.UUIDField(default=uuid.uuid4, unique=True, editable=False)` |
| `source_sha256` (L164) | `models.CharField(max_length=64)` |
| `document` (L165) | `models.JSONField()` |
| `mapping` (L166) | `models.JSONField(default=dict)` |
| `preview` (L167) | `models.JSONField(default=dict)` |
| `state` (L168) | `models.CharField(max_length=12, default='STAGED', choices=[(s, s) for s in ('STAGED', 'READY', 'COMPLETED', 'CANCELLED')])` |
| `approval_digest` (L169) | `models.CharField(max_length=64, blank=True)` |
| `result` (L170) | `models.ForeignKey('loans.HistoricalLoanImport', null=True, on_delete=models.PROTECT)` |
| `created_by` (L171) | `models.ForeignKey(settings.AUTH_USER_MODEL, on_delete=models.PROTECT, related_name='+')` |
| `created_at` (L172) | `models.DateTimeField(auto_now_add=True)` |

Methods/properties: none declared; inherited behavior still applies.
