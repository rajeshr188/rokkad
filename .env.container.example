# Local Docker only. Copy to .env.container and replace every placeholder.
# Pass explicitly: docker compose --env-file .env.container ...
SECRET_KEY=replace-with-a-long-random-development-secret
CONTAINER_OWNER_PASSWORD=replace-with-a-separate-owner-password
DB_RUNTIME_PASSWORD=replace-with-a-separate-runtime-password
